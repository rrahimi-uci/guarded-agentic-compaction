# improve-iclr.md — Execution plan for the ICLR 2027 revision of *From Traces to Guarded Programs*

Prepared 2026-09-21 from `paper/GAC_ICLR_2027_Detailed_Revision_Plan.docx` (the PAT/OpenReview feedback blueprint, forum `DF0JaS58gr`) and a full audit of the repository's retained results, scripts, library source, and artifact pipeline. This document is a research-engineering specification: every task states what to change, where, how to verify it, and what counts as done. It does not restate the feedback; it turns it into work.

---

## 0. How to execute this document

### 0.1 Target manuscript

| Item | Value |
|---|---|
| Submission source | `paper/iclr/` (`main.tex`, `sections/*.tex`, `appendix.tex`, `tables/*.tex`, `figures/*.tex`, `figures/*.pdf`) |
| Compiled PDF (tracked) | `paper/iclr/build/main.pdf` — 25 pages; main text ends on page 9 with zero slack; AI Use Statement starts page 10 |
| Long-form article (shares claims, not the target) | `paper/tex/body.tex`, `paper/tex/abstract-body.tex` → `paper/open_research/{article,main}.pdf` |
| Feedback source | `paper/GAC_ICLR_2027_Detailed_Revision_Plan.docx` (untracked at time of writing; see T0.1) |

Page/label map of the current build (use these when the feedback says "Table 10" or "Appendix H"):

| Label in feedback | LaTeX source | PDF page |
|---|---|---|
| §2 Problem Formulation | `sections/problem.tex` | 3 |
| §3 Method, Stage 6, Proposition 1 | `sections/method.tex` | 4–6 |
| Alg. 1 Compile, Alg. 2 Calibrate | `figures/alg-compile.tex`, `figures/alg-calibrate.tex` | 4, 6 |
| §4 Setup | `sections/evaluation.tex` | 7 |
| §5.1 / Table 1 | `sections/results.tex`, `tables/primary_results.tex` | 7 |
| §5.2 | `sections/results.tex` | 8 |
| §5.3 / Table 2 | `sections/results.tex`, `tables/refusal_funnel.tex` | 8 |
| §6, §7 | `sections/related_work.tex`, `sections/discussion.tex` | 9 |
| Appendix A Proof + Corollary 2 | `appendix.tex` `\section{Proof ...}` `app:proof` | 13 |
| Appendix B Alg. 3 BuildPatg, Alg. 4 Dispatch | `appendix.tex` `app:algorithms`; `figures/alg-patg.tex`, `figures/alg-dispatch.tex` | 14–15 |
| Appendix C Configuration, Table 3 (DSL) | `appendix.tex` `app:config` | 15–16 |
| Appendix D Paired statistics, Table 4 (manual) | `appendix.tex` `app:stats`; `tables/manual_resources.tex` | 17 |
| Appendix E Additional detail, Table 5 | `appendix.tex` `app:detail` | 17 |
| Appendix F Evidence breadth, Tables 6–9 | `appendix.tex` `app:breadth`, `app:external` | 18–21 |
| Appendix G Extended limitations | `appendix.tex` `app:limits` | 21–23 |
| Appendix H Prospective protocol, Table 10 | `appendix.tex` `app:prospective`; `tables/gate_frontier_disaggregated.tex` | 24–25 |

### 0.2 Environment and standard commands

All commands run from the repository root unless stated.

```bash
PY=.venv/bin/python                 # Python 3.14.4; scipy 1.18.0, numpy, pandas, pyarrow available
$PY -m pytest -q                    # full suite (tests/ and paper/scripts/ are both testpaths)
$PY paper/scripts/build_artifacts.py            # figures + paper/tables + admission/cache registers
(cd paper/tex && tectonic --keep-logs --keep-intermediates --outdir ../open_research article.tex \
                && tectonic --keep-logs --keep-intermediates --outdir ../open_research main.tex)
(cd paper/iclr && tectonic --outdir build main.tex)               # ICLR blind build
pdftotext -f 9 -l 9 paper/iclr/build/main.pdf - | tail -20        # conclusion must end on page 9
pdftotext -f 1 -l 1 paper/iclr/build/main.pdf - | head -3         # must print "Anonymous authors"
pdfinfo paper/iclr/build/main.pdf | grep Author                   # must print nothing
$PY paper/scripts/finalize_manifest.py                            # publication_manifest.json checksums
$PY scripts/verify_release.py
$PY paper/scripts/validate_artifacts.py                           # must exit 0 at the end of every phase
```

Order matters: upstream analyses → `build_artifacts.py` → open_research build **with `--keep-logs`** → ICLR build → `finalize_manifest.py` → `validate_artifacts.py`. The two checks currently failing locally ("LaTeX log is not older than the PDF") are cured only by rebuilding open_research with `--keep-logs`; do not `touch` or delete the logs.

Known pipeline facts the executor must not rediscover:

- All ten `paper/iclr/tables/*.tex` files are hand-typed; `build_artifacts.py` writes only `paper/tables/`. New ICLR tables must therefore be either generated by a new script that writes into `paper/iclr/tables/` **and** validated, or hand-typed **and** validated (Task T6.2 adds the validator).
- `validate_artifacts.py` reads no file under `paper/iclr/sections` or `paper/iclr/tables`; its only ICLR check is the page-9 budget on the compiled PDF (`validate_iclr_page_budget`, ~line 1581). Numbers in the ICLR sources are unvalidated today.
- `build_artifacts.py:33` sets `ICLR_FIGURES = PAPER / "ICLR" / "figures"` (upper-case). It works on macOS only because APFS is case-insensitive. Fix in T6.3.
- Only three generated figures are used by the ICLR build: `gac_aha_example.pdf`, `family_reductions.pdf`, `gate_support.pdf`.
- Provider studies need `OPENAI_API_KEY` in `.env` and an explicit `--approved-spend-usd` where the driver supports it. Every experiment task below states its expected spend; nothing in Phases 1, 2, 5, or 6 makes a provider call.

### 0.3 Repository policy (binding)

- Work on a dedicated branch (suggested: `paper/iclr-2027-revision-p0`, `…-p1`, one branch per phase group). Never commit to `main`.
- Open a PR per phase group using `.github/pull_request_template.md` in full; request review from `rrahimi-uci`; do **not** request a Copilot review; never merge.
- Never overwrite a retained result file under `paper/results/`. New runs write to new directories named in each task. Failed or retired outcomes are committed, not deleted.
- Never change `alpha`, `delta`, the grid `Lambda`, the candidate set, or a cohort after seeing an outcome. Retirement is reported as retirement.

### 0.4 Deadline assumption and tracks

`paper/iclr/notes/iclr_2027_compliance_checklist.md:68-69` records the ICLR 2027 full-paper deadline as **2026-09-25 AOE** (checked 2026-08-14). Today is 2026-09-21. The executor must re-verify the date before planning spend. Tasks carry a track label:

- **Track S (submission):** must land before the paper deadline. Writing-only corrections and provider-free reanalysis. Roughly 48–72 hours of work.
- **Track R (rebuttal/camera-ready):** new experiments and code changes whose result can be added during the discussion phase. Each Track R experiment is pre-registered in Track S (protocol file committed) so the submission can cite it as "pre-registered, running".
- **Track F (future work):** designed here at the level of a protocol but explicitly not executed; the paper names it as future work.

If the deadline turns out to be later than assumed, promote Track R tasks marked "promotable" into Track S in the order T3.1, T3.2, T3.3.

### 0.5 Status legend and task-card format

Every concern and task carries exactly one evidence status:

| Tag | Meaning |
|---|---|
| `SUPPORTED` | Already supported by retained data; only presentation changes |
| `REANALYSIS` | Requires recomputation from retained data; no provider call |
| `NEW-EXP` | Requires a new experiment (provider calls or new driver) |
| `WRITING` | Requires only a writing/clarification change |
| `NARROW` | Cannot currently be supported; the claim must be narrowed |

Task cards use the fields: Priority (P0/P1/P2), Track, Status, Depends-on, Concerns addressed (RC-ids from §1), Issue, Existing evidence, Gap, Steps, Files, Output artifacts, Paper location, Completion criterion, Verification.

### 0.6 Verified arithmetic (recomputed 2026-09-21; cite these, do not re-derive)

Setting: `alpha = 0.05`, `delta = 0.1`, `|Lambda| = 11`, zero violations. `U(n, m) = 1 - (delta / (m·|Lambda|))^(1/n)`.

| Quantity | Value |
|---|---|
| `U(92, m=1)` | 0.049809 (admits) |
| `U(91, m=1)` | 0.050342 (retires) |
| `U(92, m=2)` | **0.056941 (retires at alpha = 0.05; admits at alpha ≥ 0.057)** |
| `U(105, m=2)` | 0.050071 (retires) |
| `U(106, m=2)` | 0.049610 (admits) |
| `n_min(m)` for m = 1, 2, 3, 4, 8, 16 | 92, 106, 114, 119, 133, 146 |
| Certifiable coverage floor `ceil(92/c)` for c = 1, 0.8, 0.7, 0.5, 0.25, 0.1 | 92, 115, 132, 184, 368, 920 |
| One-sided 95% Clopper–Pearson upper bound, 0/90 | 0.0327 (the "3.3%" in §5.1) |
| Same, 0/30; 0/240; 0/239; 1/18 | 0.0950; 0.0124; 0.0125; 0.2377 |
| `frozen_candidate_coverage_simulation.json` worst-case single-threshold miscoverage | 0.00909 at `r* = 0.6365`; per-threshold union over m candidates 1.81% (m=2) … 13.59% (m=16); Bonferroni-corrected ≤ 0.81% for every m |

### 0.7 Non-negotiable rules for every executor

1. Do not invent numbers. Every number entering the manuscript must have a JSON key path or a script that regenerates it, recorded in the task's completion note.
2. Prefer the retained file over memory. If a fact here disagrees with a retained file, the file wins; record the discrepancy in `paper/iclr/notes/revision_log.md` (created in T0.1).
3. Keep the page-9 budget: any main-text insertion must be paired with a listed cut (see T4.6).
4. Where a task offers Track A / Track B language, the manuscript must use exactly one, and the same one everywhere (abstract, §3, §5.1, §7, Appendix A, Appendix C).

---

## 1. Reviewer-concern register

Each row is one substantive concern from the feedback blueprint (its section in parentheses) or an independent finding the audit surfaced while checking the blueprint's claims against retained data. "Evidence status" uses the §0.5 legend. Items the blueprint marks RESOLVED (§2.1 of the docx: prefix-only scope, baseline-coupled loss, group measure in Eq. 4, single-source Eq. 1, 92-group floor, nonconformity model, 23-operator DSL, ρ ambiguity, manual comparator metrics, 34-vs-11 AppWorld counts, pytorch retirement mechanism, ReAct limitation, comparator context, cluster mitigations) get **no task** beyond the Phase 5 consistency re-check; do not reopen them.

| ID | Concern (blueprint §) | Why it matters | Evidence status | Resolution path |
|---|---|---|---|---|
| RC-1 | Candidate-selection multiplicity: PR-outcome and backlog each calibrated two candidates on one 92-group split and kept the survivor after seeing outcomes; Proposition 1 covers one fixed candidate (§3.1, P0) | The headline live deployment claim is not covered by the theorem actually proved; the abstract currently implies certificates without saying which level | `REANALYSIS` now (Bonferroni over m=2 on retained tables gives U=0.0569 > 0.05, so `NARROW` for those two families at α=0.05); `NEW-EXP` to repair (fresh 106-group cohort, Track R); issue-type is `SUPPORTED` at m=1 because its second candidate retired at synthesis and never saw calibration | T2.3, T3.1, T4.1 |
| RC-2 | Appendix H denominators: text says 240/240 and 239/239, Table 10 sums to 239/240 (§3.5, P0) | Arithmetic inconsistency in a headline appendix result; retained data show 240/240 baseline, 240/240 learned gate, 239/240 support-only (one support-only episode on `psf/requests` #6708 timed out; that record's other arms completed) | `SUPPORTED` (retained per-episode data) + `WRITING` | T1.1, T2.8 |
| RC-3 | Algorithm 3 uses `u` as slot name and value, says "block region" inside PATG, does not bind `o_j`, order edges lack `s<t` (§4.1, P0) | Published pseudocode does not match the implementation (which annotates slots and lets the window miner block) or the formal scope | `WRITING` (implementation already matches the proposed semantics) | T1.4 |
| RC-4 | Algorithm 4: no explicit `boundary_index = 0` check, runtime inputs unbound, `quota_attested` undefined, post-commit branch (§4.2, P0) | Eq. (2) is not visibly enforced; a prologue call by a different tool passes the intersection test | `WRITING` for inputs/definition; small `NEW-EXP`-free code change to enforce `already_observed = ∅` at runtime (Track S, provider-free) | T1.5, T3.6 |
| RC-5 | Appendix A multiplicity paragraph reports 1.8%–13.6% as if grid-wide (§3.4, P0) | Invites a valid statistical objection; the simulation script computes per-threshold unions over m candidates, not grid-wide unions | `SUPPORTED` (script semantics verified) + `WRITING` | T1.3 |
| RC-6 | 92-group step gate over-interpreted; Appendix H reads an infeasible design as a negative learned-gate result (§3.2, §3.5, P1) | With |K|=92 the only certifiable coverages are 0 and 1; the pre-registered "three admissible nonzero levels" criterion was unattainable; the protocol file never states this | `WRITING` + `REANALYSIS` (held-out `q` is constant per repository; dispatch coverage was 160/240, never reported) | T1.2, T2.8, T4.3 |
| RC-7 | No unguarded recurrence-only ablation (§5.1, P1) | Cleanest answer to "are the guards doing anything?"; the paper names it as the highest-value missing comparison | `SUPPORTED` for a mechanism-removal table (eight retained failure cases each caught by a named guard); `NEW-EXP` for a live B1 arm (issue-type only, where B1 ≠ compiled) and for the perturbation arms (Track R) | T2.10, T3.2, T3.3 |
| RC-8 | Percent-only reporting; no cost CIs; no issue-type inferential rows; no discordant cells; no robust latency summary (§5.3, P1) | Percentages are hard to audit; tiny absolute dollar values matter for practical significance | `REANALYSIS` (all per-record fields retained for 90 records × 3 arms) | T2.1, T2.2 |
| RC-9 | Reproducibility manifest: snapshot, SDK versions, effective generation settings, retry/timeout, cache policy, ordering, concurrency, pricing formula (§5.2, P1) | "Sampling parameters take provider defaults" is not reproducible | `SUPPORTED` (facts are in retained `run` blocks and code constants) + `WRITING` | T2.5 |
| RC-10 | i.i.d. group assumption: `min_days = min_principals = 1`; "distribution-free" wording (§3.3, P1) | Guarantee is weakest where the headline is strongest | `REANALYSIS` (compute effective independent units by day and by author from the pinned snapshot; conservative cluster-level bound) + `WRITING` (no "distribution-free" occurrence exists in the ICLR source; keep it that way) | T2.9, T4.4 |
| RC-11 | 90/90 vs 89/90 read as accuracy gain (§2.3) | One baseline-only miss on a stochastic baseline is not superiority | `SUPPORTED` (the miss is backlog #5189, `comment_grounded` false after the baseline skipped the discussion read; the same 30 records rerun on 2026-08-18 scored baseline 30/30) + `WRITING` | T1.8 |
| RC-12 | Latency validity: order, region, concurrency, warm-up, caching (§2.3) | Wall-time reductions can mix structure with service variance | `REANALYSIS` (per-record schedule retained; recompute reductions within each of the six order permutations) + `WRITING` | T2.4 |
| RC-13 | Live effect-catalog trust root for GitHub tools (§4.4, P1) | External substrates got empirical audits; the live catalog did not | `SUPPORTED` (catalog declared in `make_catalog()`, digest pinned in every manifest, tools are pure reads of a pinned parquet; 90/90 tool contracts) + `WRITING` | T2.6 |
| RC-14 | "Independent five-times-larger cohort" (§2.3) | Record pools overlap the core cohort heavily (calibration 442/460, test 130/150 shared); 240 vs 120 is 2×, not 5× | `NARROW` + `WRITING` | T1.6, T4.5 |
| RC-15 | Comparator positioning; Headroom null must not stand in for a reuse baseline (§5.4, P2) | Overuse of a null engagement | `WRITING` | T4.2 |
| RC-16 | Read-only prologue for ReAct dispatchability (§4.3, P2 optional) | High upside, changes the runtime adapter | `NEW-EXP`, Track F (protocol only) | T3.5 |
| RC-17 | Larger calibration pool for a graded frontier (§3.2, ambitious) | Needs |K| ≥ 184 for a 50% point | `NEW-EXP`, Track F (protocol only) | T3.4 |
| RC-18 | Editorial ledger: lowercase "proposition 1"/"corollary 2" in PDF, bare `.11`, "behaviour", "both GitHub families", `INSERT/UPDATE/DELETE/ CREATE`, H.1 cross-reference to §7, Basu orphan, `.05`/`.0498` in Figure 1 (Appendix A of docx) | Reviewer trust | `WRITING` | T1.6, T1.10 |
| RC-19 | Nine-page budget after insertions (§6 of docx) | Any main-text growth breaks compliance | process | T4.6, T6.1 |
| RC-20 | Abstract/TL;DR/OpenReview metadata alignment (§9.4 of docx) | Scope drift across surfaces | `WRITING` (user action for OpenReview fields) | T4.7 |
| RC-21 (audit finding) | Appendix H says the `psf/requests` result was produced by the five-repository run; retained timestamps show the block is the single-repository smoke run reused through the `--resume` short-circuit; the protocol text calls the smoke run "not the pre-registered evidence" | Undisclosed reuse is a protocol-integrity gap a reviewer can find from the JSON | `SUPPORTED` + `WRITING` (disclose; do not rerun before the deadline) | T1.1 |
| RC-22 (audit finding) | Held-out dispatch coverage in the cross-repository core and gate-frontier studies is never reported: 160/240 compacted, 80/240 fell back on the guard clause `range:pr.state` (every open-class PR) | "Completes 120/120 exact pairs" is true but hides that a third of records ran the unchanged agent; also explains 44.4% vs 66.7% request reductions | `REANALYSIS` + `WRITING` | T2.8, T4.5 |
| RC-23 (audit finding) | Table 10 caption: "Support-only (α=1) produces the identical sweep and admission on every repository" — the support-only grid has 12 points, so U differs (0.0507 vs 0.0498) and the deployed threshold is 1.0, not 0.5 | Caption is factually wrong in detail | `WRITING` | T1.1 |
| RC-24 (audit finding) | Appendix H "four times the prior cross-repository scale" and protocol "$0.41 including retries" | 240 vs 120 is 2×; retained successful-attempt cost sums to $0.4058 and failed attempts are not retained | `WRITING` | T1.1 |

---

## 2. Evidence inventory (facts the executor relies on; all verified against retained files on 2026-09-21)

### 2.1 Primary GitHub families (§5.1, Table 1, Appendix D)

| Fact | Where |
|---|---|
| Per-record `metrics` for all 90 records × {baseline, compiled, manual}: `requests, tool_calls, input_tokens, output_tokens, cached_input_tokens, cache_write_tokens, total_tokens, wall_latency_ms, provider_response_latency_ms, estimated_cost_usd` | `paper/results/github_workflow_families/pr_outcome/final/results.json` → `results[]`; `…/backlog_attention/final/results.json` → `results[]`; `paper/results/github_natural_replication/results.json` → `results[]` (use `repeat == 0`; "manual" is `condition == "macro"`) |
| Ledger used by Table 1 (ratio-of-sums reductions) | `paper/results/github_workflow_families/summary.json` (`overall`, `families[]`) |
| The single baseline miss | backlog `issue_number 5189`, `condition baseline`, `quality.comment_grounded false`, `tool_sequence` has two calls (skipped `backlog_get_discussion`); `quality.tool_contract true` |
| Independent rerun of the same 30 records (2026-08-18) | `paper/results/github_workflow_families/{pr_outcome,backlog_attention}/headroom_ablation/results.json`: baseline 30/30, compiled 30/30, manual 30/30 in both families |
| Candidates per family | pr and backlog: `compiler.report` "candidates mined 2 / artifacts emitted 1"; the two-read candidate is `[dominated] … REJECTED: dominated_by_longer_region` **after** calibration; issue-type: `compiler.candidates[0]` (three-read) `stage synthesized`, `rejected ungroundable_slot:issue_get_comments.limit`, `gate: null` — it never reached calibration; `candidates[1]` admitted |
| Per-threshold calibration table for the emitted artifact | `compiler.artifact.gate.notes` (string, full grid) and `compiler.artifact.gate.{grid, admissible, threshold, n_accepted, n_calibration_groups, observed_violations, risk_upper_bound}`; pr/backlog: n=0 for η ≤ 0.11, n=92 for η ≥ 0.14; issue-type admissible from 0.11 |
| Dominated candidate's calibration table | **not retained** in family results; regenerable provider-free by recompiling from the discovery checkpoint (`reconstruct_discovery()` in `paper/scripts/github_workflow_family_study.py` ~1222 + `paper/results/datasets/github_issues/train-00000-of-00001.parquet`) |
| Splits | `compiler.splits.sizes {train 16, dev 8, calibration 92}`; group ids in `compiler.splits.groups.*` and `compiler.artifact.evidence.split_ids`; seed 20260805 (pr, backlog) / 20260802 (issue-type); `min_days 1, min_principals 1, max_candidates 16, max_artifacts 4, prefix_only true`, `freeze_one_candidate_before_calibration` **not set (False)** |
| Condition order | pr/backlog: `schedule[i].order` = permutation `i % 6` of (baseline, compiled, manual_pre_model); arms run sequentially per record (`concurrency=1`); issue-type: blocked six-permutation Latin design (`condition_order.primary`), concurrency 8 |
| Provider settings | `run.model gpt-5.6-luna`, `run.openai_agents_sdk 0.19.2`, `run.openai_python 2.52.0`, `run.python 3.14.4`, `run.platform macOS-26.5.2-arm64`; `ModelSettings(reasoning=Reasoning(effort="low"), verbosity="low", parallel_tool_calls=False, store=False)` in `paper/scripts/github_live_study.py:522-530`; temperature/top_p unsupported by the model (`run.temperature` in issue-type file); `max_turns=8`, `timeout=120.0` s per episode, **no retries**; price table `demos/live_runtime.py:58-65` (retrieved 2026-08-02) |
| Tool outputs | not retained; exactly reconstructible from `tool_sequence` + `tool_arguments` + the pinned parquet via `execute_snapshot()` |
| Effect catalog for live tools | `make_catalog()` in `paper/scripts/github_workflow_family_study.py:506-520` (capabilities `speculatable, replayable, cacheable, batchable`); `catalog_version` digests pinned in every artifact manifest (e.g. `github-pr_outcome-pinned-reads@1#821a5aef76e5ebf1`) |

### 2.2 Cross-repository studies (§5.2, Appendix F.1, Appendix H)

| Fact | Where |
|---|---|
| Frozen search enabled | `paper/scripts/github_multirepo_pr_outcome_core.py:1089` and `paper/scripts/github_multirepo_gate_frontier_study.py:157` set `freeze_one_candidate_before_calibration=True`; both `perturbations=()`, `sandbox=None` |
| Gate-frontier arms and counts | `paper/results/github_multirepo_gate_frontier/results.json`: `aggregate.baseline.n 240`, `aggregate.learned_gate.n 240`, `aggregate.support_only.n 239`; `failures[0] = {condition support_only, error "TimeoutError: ", record_number 6708, repository psf/requests}`; #6708 passed in baseline and learned-gate arms; `run.comparative_claim_allowed false` |
| `psf/requests` block provenance | `repositories["psf/requests"].run.timestamp_utc 2026-08-22T18:59:22Z` with `resolved_config.repositories ["psf/requests"]` (smoke run), other repos 21:32–22:43Z; reused via `if args.resume and result_path.exists(): return …` |
| Held-out dispatch | per repo `results[].dispatch.outcome`: 40 COMPACTED (requests 1), 20 BASELINE with `dispatch.reasons ["range:pr.state"]` (the 20 open-class PRs); pooled 160/240 learned, 159/239 support-only; `dispatch.q` constant per repo (0.0169 huggingface, 0.0815 others) |
| Calibration sweeps | `repositories[repo].coverage_curves.{learned_gate,support_only}[]` rows `{eta, n, violations, upper, coverage}`; |K| = 92 everywhere; huggingface n=10 (coverage 0.1087, U 0.375) at η ≤ 0.08; support-only uses a 12-point grid (α=1 appends 1.0) so U=0.0507 and threshold 1.0 |
| pytorch retirement | `repositories["pytorch/pytorch"].error` (truncated string): "candidates mined 1 / artifacts emitted 0 / rejections by stage {'calibrate:retire': 1} / windows accepted 16 … upper bound is 1.000"; no per-η rows retained |
| Cohort overlap with core | test 130/150 shared; discovery 561/580; calibration 442/460 (`selection.excluded_prior_record_numbers` is 0 for four repos); same seed 20260807 |
| Resource sums | pooled baseline 720 requests / 358,011 tokens / $0.0919; learned 400 / 170,691 / $0.0475; support 399 / 169,683 / $0.0469 |
| Protocol | `paper/supplementary/prospective-gate-frontier-protocol.md`: decision rule rows at lines 273–278; no `⌈92/c⌉` statement; no timeout/retry policy; "Observed results" lines 70–163 contain the sentences listed in T1.1 |

### 2.3 Statistics and simulation

| Fact | Where |
|---|---|
| Exact bound | `src/guarded_agentic_compaction/grc/calibrate.py:67-98` (`clopper_pearson_upper`), `:390` (`conf = 1 - delta/len(grid)`); no candidate-multiplicity parameter |
| Selection rule | `calibrate.py:395-420`: last admissible η in ascending grid (= lexmax coverage, then η); coverage denominator = all source groups (`:389`) |
| Dominance | `grc/compile.py:592-624` `_select_artifacts`: sort by `(-removed_requests, -support_groups, name)`; prefix-relation dominance |
| Bootstrap/McNemar/Wilcoxon used by the studies | `paper/scripts/github_live_study.py:1215-1222` (iid percentile bootstrap, 10,000 resamples, seed 20260802), `:1253` (Wilcoxon `zsplit`), `:1280-1292` (exact McNemar via `binomtest`) |
| Simulation | `paper/results/frozen_candidate_coverage_simulation.json`: `part_one_exact_multiplicity.candidate_rows[]` (`uncorrected_union_miscoverage` 0.01810 at m=2, 0.13593 at m=16; `bonferroni_corrected_union_miscoverage` ≤ 0.00809), `worst_case_population_rate 0.6365`, `worst_case_single_candidate_miscoverage 0.00909`; `part_two_clustered_groups_monte_carlo.worst_clustered_miscoverage 0.562585` at `{block_size 46, p_bad_block 0.25, bad_shift 0.3}`, seed 20260822. The union is over candidates at **one** threshold (`union_miscoverage_probability(p, m) = 1-(1-p)^m`) |
| Gate selectivity | `paper/results/gate_selectivity_analysis.json`: 7 registered artifacts all `step_all_or_none`; five share nonzero weights `hull_margin = provenance_ambiguity = 0.8009`; partial frontiers only in the archived 2026-08-03 pilot (α=0.05, exploratory) and GCS (α=0.10) |

### 2.4 Runtime and provenance implementation

| Fact | Where |
|---|---|
| Runtime prefix enforcement | `runtime/dispatch.py:264-268` checks only `tools(P) ∩ already_observed`; compile-time `GrcConfig.prefix_only=True` keeps windows with `a == 0` (`graph/windows.py:500-504`); no runtime `boundary_index` |
| `quota_attested` | `schema/effects.py:212`, a boolean `EffectSpec` field (not a `Capability`); used in `dispatch.py:269-279` in LIVE mode when `snapshot_fn` is absent |
| Pre/post-commit | `runtime/interp.py:31-36` defines both errors; `PostCommitError` is never raised; staging refuses any non-read effect at commit → INCIDENT (`staging.py:94-98`) |
| PATG marks | `graph/provenance.py:124-129` `GROUNDED, UNGROUNDED, AMBIGUOUS, LITERAL, NOT_GROUNDABLE`; blocking in `graph/windows.py:310-317`; order edges `i < j` (`provenance.py:295-303`); up to κ witnesses kept, single source chosen in `grc/synthesize.py:123-175` |
| Perturbation suite | `evaluation/perturb.py:190-200`, nine families; not run in any GitHub study |
| Signing | `registry/store.py:42` `signing_key=b""` default → unsigned, unverified in reported runs |

---

## 3. Phase plan

Dependencies are listed per task. Within a phase, tasks without a listed dependency may run in parallel. Phase 0 precedes everything; Phase 6 closes every PR.

### Phase 0 — Freeze and baseline (Track S, day 0)

#### T0.1 — Archive the feedback and open the revision log
- Priority P0 · Track S · `WRITING` · Depends on: none · Concerns: process
- Steps:
  1. `git checkout -b paper/iclr-2027-revision-p0` from `main`.
  2. `mv paper/GAC_ICLR_2027_Detailed_Revision_Plan.docx paper/reviews/ && git add paper/reviews/GAC_ICLR_2027_Detailed_Revision_Plan.docx` (the file is untracked today) and export `pandoc paper/reviews/GAC_ICLR_2027_Detailed_Revision_Plan.docx -t gfm --wrap=none -o paper/reviews/GAC_ICLR_2027_Detailed_Revision_Plan.md` so the feedback is greppable.
  3. Create `paper/iclr/notes/revision_log.md` with a header, the baseline facts below, and one dated entry per task as it completes (task id, files touched, numbers changed with JSON key paths, verification command output summary).
  4. Record baseline hashes: `shasum -a 256 paper/iclr/build/main.pdf paper/open_research/*.pdf paper/results/publication_manifest.json` into the log.
- Completion criterion: both review files tracked; `revision_log.md` exists with the baseline block; `git status` clean apart from intended files.

#### T0.2 — Baseline verification run
- Priority P0 · Track S · Depends on: T0.1
- Steps: run `$PY -m pytest -q` (expect all passing), `$PY paper/scripts/validate_artifacts.py` (expect exactly the two known log-mtime failures), `pdfinfo paper/iclr/build/main.pdf` (expect 25 pages), and the page-9 command from §0.2. Paste the four outputs into `revision_log.md`.
- Completion criterion: outputs recorded; no unexpected test failure. If a test fails, stop and report — do not proceed with a red baseline.

### Phase 1 — Paper-logic corrections (Track S, writing-only, P0)

All Phase 1 tasks edit `paper/iclr/` only, make no provider call, and are verified by rebuilding the ICLR PDF and grepping the source. Their combined main-text footprint must be ≤ 0 lines (see T4.6).

#### T1.1 — Correct Appendix H accounting, provenance, and captions
- Priority P0 · Track S · `SUPPORTED` + `WRITING` · Depends on: T0.2 · Concerns: RC-2, RC-21, RC-23, RC-24
- Issue: `appendix.tex:789-791` says "240/240, 239/239 after one held-out record failed under a transient provider timeout independent of condition or repository"; Table 10 shows `psf/requests … 59/60` in a single Exact column; the caption claims support-only produces "the identical sweep and admission"; the text says "four times the prior cross-repository scale" and the cohort is "independent". Retained data (§2.2) contradict each of these.
- Steps:
  1. Replace the "Observed results" subsection body (`appendix.tex`, from `\subsection{Observed results}` to the end of the file) with Block H-1 in §5 of this document. Keep the `\label{tab:gate-frontier-disaggregated}` table float but replace its caption and its input file as below.
  2. Rewrite `paper/iclr/tables/gate_frontier_disaggregated.tex` to the following columns and rows (numbers from `paper/results/github_multirepo_gate_frontier/results.json`, key paths in §2.2):

     | Repository | Held-out | Exact: base / learned / support | Dispatched (learned) | Coverage in sweep | Admitted | Req. ↓ |
     |---|---|---|---|---|---|---|
     | huggingface/datasets | 60 | 60/60 · 60/60 · 60/60 | 40/60 | 0.1087 (U=0.375, rejected), 1.0 | 1.0 | 44.4% |
     | pandas-dev/pandas | 60 | 60/60 · 60/60 · 60/60 | 40/60 | 1.0 | 1.0 | 44.4% |
     | psf/requests | 60 | 60/60 · 60/60 · 59/60† | 40/60 | 1.0 | 1.0 | 44.4% |
     | streamlit/streamlit | 60 | 60/60 · 60/60 · 60/60 | 40/60 | 1.0 | 1.0 | 44.4% |
     | pytorch/pytorch | 60 sealed, 0 run | RETIRE at compile | — | none (U=1 at every η) | — | — |

     Footnote †: "one support-only episode (record 6708) raised a 120 s provider timeout and was not re-attempted; the record passed in the other two arms." Caption: "Disaggregated outcomes for the gate-frontier study. Exact contracts are reported per arm under intention-to-treat. `Dispatched` counts held-out records on which the compiled program ran; the remainder fell back to the unchanged agent on the guard clause `range:pr.state`. `Coverage in sweep` lists every calibration coverage the frozen grid produces for the learned gate; the support-only arm (α=1) reproduces the same n and k at every η but, because α=1 appends η=1.0 to the grid, its Bonferroni split is δ/12 (U=0.0507 at n=92) and it deploys threshold 1.0 rather than 0.5."
  3. Replace "independent five-times-larger cohort" (`appendix.tex:771`) with the wording in Block H-1 (shared-pool, twice the core scale). Replace "four times the prior cross-repository scale" (`:808`) likewise.
  4. Mirror the same corrections in `paper/supplementary/prospective-gate-frontier-protocol.md` "Observed results" (lines 70–163): the `$0.41 … including two retries` sentence becomes "$0.4058 summed over retained successful attempts; the two failed `streamlit/streamlit` discovery attempts are not retained and their spend is unrecorded"; delete the claim that the smoke-check failure was reproduced in the full run (it is the same event); replace the independence and 5× sentences; state the `psf/requests` reuse. Also add one paragraph "Retrospective note" stating that the protocol fixed no timeout/retry rule and did not state the ⌈92/c⌉ constraint.
  5. Mirror in `paper/tex/body.tex:2857-2875` and `paper/supplementary/evidence-register.md:24-25` (the validator reads body.tex; keep its phrases consistent — see T5.3).
- Files: `paper/iclr/appendix.tex`, `paper/iclr/tables/gate_frontier_disaggregated.tex`, `paper/supplementary/prospective-gate-frontier-protocol.md`, `paper/tex/body.tex`, `paper/supplementary/evidence-register.md`.
- Completion criterion: `grep -n "239/239\|240/240\|five-times\|four times" paper/iclr/appendix.tex paper/tex/body.tex` returns no hit except inside Block H-1's "240/240 baseline, 240/240 learned-gate, 239/240 support-only"; Table 10 row sums equal 240/240, 240/240, 239/240; T6.2's new validator check passes.

#### T1.2 — Fix the Appendix H decision-rule cross-reference and interpretation
- Priority P0 · Track S · `WRITING` · Depends on: T1.1 · Concerns: RC-6
- Steps:
  1. `appendix.tex:807` "Per the decision rule fixed in advance (\cref{sec:discussion})" → "Per the decision rule fixed above in this appendix". Block H-1 already contains the corrected sentence; confirm no other `\cref{sec:discussion}` in `app:prospective` refers to the decision rule (lines 711 and 813 refer to the step-gate finding and are correct).
  2. Ensure Block H-1's final paragraph states verbatim that the "three admissible nonzero coverage levels" criterion was unattainable at |K|=92 and that q is constant on held-out records.
- Completion criterion: `grep -n "fixed in advance" paper/iclr/appendix.tex` shows the new wording; the PDF's Appendix H no longer cites §7 for the decision rule.

#### T1.3 — Rewrite the Appendix A multiplicity paragraph
- Priority P0 · Track S · `SUPPORTED` + `WRITING` · Depends on: T0.2 · Concerns: RC-5, RC-1
- Issue: `appendix.tex:57-75` reports "1.8% at m=2 to 13.6% at m=16, crossing δ=0.10" as if it were the grid/candidate-wide certificate; `frozen_candidate_coverage_simulation.py` unions m candidates at a single threshold (`union_miscoverage_probability(p, m) = 1-(1-p)^m`, no union over Λ). The "Two scope conditions" paragraph also says the three primary families each calibrate two candidates; issue-type calibrated one.
- Steps:
  1. Replace the paragraph beginning `\code{paper/scripts/frozen\_candidate\_coverage\_simulation.py} backs this` through `this guarantee's weakest link.` with Block A-1 (§5).
  2. Replace, in the sentence after Corollary 2, "The three primary GitHub families do not use it --- each mines two candidates that both reach calibration, with the higher-removal survivor kept by dominance --- so they remain the per-candidate certificates above and are not retroactively strengthened." with Block A-2 (§5), which distinguishes issue-type (m=1) from PR-outcome and backlog (m=2) and states U(92, m=2)=0.0569.
  3. In `main.tex`, after `\newtheorem{corollary}[proposition]{Corollary}`, add `\crefname{proposition}{Proposition}{Propositions}` and `\crefname{corollary}{Corollary}{Corollaries}` so mid-sentence `\cref` renders capitalized (PDF currently prints "proposition 1" at five places).
  4. Also update the hand-written `paper/tables/frozen_candidate_simulation.tex` header "Uncorrected union miscoverage" → "Uncorrected single-threshold union miscoverage over m candidates" and the body.tex mirror at `body.tex:951-967`.
- Completion criterion: `pdftotext paper/iclr/build/main.pdf - | grep -c "proposition 1"` → 0 (all capitalized); Appendix A contains the four-line γ/δ/m derivation; the words "single-threshold" appear next to 1.8% and 13.6%.

#### T1.4 — Patch Algorithm 3 (BuildPatg) to the implemented semantics
- Priority P0 · Track S · `WRITING` · Depends on: T0.2 · Concerns: RC-3
- Existing evidence: `graph/provenance.py` distinguishes slot path from value, marks slots GROUNDED/UNGROUNDED/AMBIGUOUS/LITERAL/NOT_GROUNDABLE, never blocks; `graph/windows.py:310-317` blocks windows containing UNGROUNDED/AMBIGUOUS slots; order edges use `i<j`; up to κ witnesses are kept and synthesis commits to one source.
- Steps:
  1. Replace the body of `paper/iclr/figures/alg-patg.tex` with Block ALG-3 (§5).
  2. In `paper/iclr/figures/alg-compile.tex` line `\State $G\gets\textsc{BuildPatg}(...)$` change to `\State $(G,S)\gets\textsc{BuildPatg}(\mathcal{T},\Theta,\mathcal{C},\mathcal{L},\kappa)$ \Comment{\cref{alg:patg}: annotate every slot}` and the next line to `\State $\mathcal{F}\gets\textsc{Canonicalize}(\textsc{MineRegions}(G,S,\mathcal{C},w_{\max}))$ \Comment{rejects any window with an \textsc{ungrounded} or \textsc{ambiguous} slot; keeps cross-group support}`.
  3. In `appendix.tex` `app:algorithms` first paragraph, replace "Its two block conditions are asymmetric on purpose" sentence with: "\patg annotates; \textsc{MineRegions} blocks. The two blocking marks are asymmetric on purpose: a slot with no witness is a genuine model decision and a slot with more than κ witnesses is ambiguous, so the analysis never resolves an argument by picking the cheapest explanation. \patg keeps up to κ single-source witnesses per grounded slot and \textsc{Synthesize} commits to the one source stable across every supporting group."
  4. In `sections/method.tex` Stage 1 paragraph, "Two outcomes block a region rather than guessing" → "Two slot marks make the region miner block rather than guess".
- Completion criterion: rendered Algorithm 3 shows `v_{j,s}`, `S[j,s]`, `(c_j,o_j)`, `s<t`, returns `(G,S)`, and contains no "block region" line; Algorithm 1 consumes `(G,S)`.

#### T1.5 — Patch Algorithm 4 (Dispatch): bound the runtime context, enforce position 0, define the capability
- Priority P0 · Track S · `WRITING` (+ code in T3.6) · Depends on: T0.2; must ship in the same PR as T3.6 · Concerns: RC-4
- Existing evidence: `runtime/dispatch.py:177-191` takes `already_observed` and `snapshot_fn` explicitly and builds `Staging` internally; `quota_attested` is an `EffectSpec` boolean (`schema/effects.py:212`); `PostCommitError` is never raised; staging commit refuses non-read effects → INCIDENT.
- Steps:
  1. Replace `paper/iclr/figures/alg-dispatch.tex` with Block ALG-4 (§5). Changes: `X=(M',\mathcal{O},\textsc{snap},\textsc{stage})` in the input line; new line `if X.O ≠ ∅ then FALLBACK` before the guard (subsumes the tool-intersection test and enforces Eq. (2) against a prologue by any tool); staging begun from `X.stage` with `X.snap`; the unreachable post-commit branch replaced by the commit-refusal line.
  2. Add to `appendix.tex` `app:config` after the admission-budget paragraph a short paragraph "Runtime capability vocabulary" (Block C-1 in §5) defining `quota_attested` as a boolean field of a tool's effect declaration, stating that position 0 is identified at runtime with "no tool observation yet in this episode", and stating that in the shipped facade every effect outside {PURE, READ_LOCAL, READ_EXTERNAL} is refused at commit so no post-commit path is reachable in the reported runs.
  3. `app:algorithms` second paragraph: after "Cheap deterministic checks reject first" add "the first of them is the position check: any tool already observed in the episode, whether or not the compiled program uses it, returns the unchanged agent (\cref{eq:prefix})."
  4. Spelling: `figures/alg-dispatch.tex:37` "behaviour" → "behavior".
- Completion criterion: rendered Algorithm 4 has no free variable (`AlreadyObserved`, `Snapshot`, `Stage` all bound through `X`), contains the explicit `X.O ≠ ∅` line, and `quota_attested` is defined in Appendix C; the unit test from T3.6 passes.

#### T1.6 — Editorial ledger
- Priority P1 · Track S · `WRITING` · Depends on: T0.2 · Concerns: RC-18, RC-14
- Each line is one edit; verify with the grep shown.

  | # | Location | Edit | Verify |
  |---|---|---|---|
  | 1 | `main.tex` | add `\crefname{proposition}…` and `\crefname{corollary}…` (done in T1.3.3) | `pdftotext build/main.pdf - \| grep -c "proposition 1"` → 0 |
  | 2 | `appendix.tex:800` `$\eta\ge.11$` | `$\eta\ge 0.11$` | `grep -n '\\ge\.' paper/iclr/appendix.tex` → none |
  | 3 | `appendix.tex:566`, `figures/alg-dispatch.tex:37`, `figures/pipeline_overview.tex:101` | "behaviour" → "behavior" | `grep -rn behaviour paper/iclr` → none |
  | 4 | `appendix.tex:629` "both GitHub families" | "the PR-outcome and backlog-attention families" | `grep -n "both GitHub" paper/iclr/appendix.tex` → none |
  | 5 | `appendix.tex:487` `\code{DELETE}/` + line break + `\code{CREATE}` | join as `\code{INSERT}/\code{UPDATE}/\code{DELETE}/\allowbreak\code{CREATE}/\code{DROP}` on one source line (`\allowbreak` gives a legal break without a space) | PDF text contains `INSERT/UPDATE/DELETE/CREATE/DROP` without a space |
  | 6 | `appendix.tex:771`, `:808` | independence / 5× / 4× wording (done in T1.1) | `grep -n "independent five\|four times" paper/iclr/appendix.tex` → none |
  | 7 | `appendix.tex:807` | cross-reference (done in T1.2) | as T1.2 |
  | 8 | `references.bib` Basu et al. | if the entry's author list still orphans one line at a page top after all edits, add `\interlinepenalty=10000` inside `\bibliographystyle` block or shorten by using `and others` only if the venue permits; otherwise leave — cosmetic | visual check of the References pages |
  | 9 | Global | `grep -rn -E '(=|<|>|\\le|\\ge)\s*\.[0-9]' paper/iclr/sections paper/iclr/appendix.tex paper/iclr/tables` → none | |
  | 10 | Global | `grep -rn -i 'distribution-free' paper/iclr` → none (already true; keep) | |
- Completion criterion: all ten verifications pass on the rebuilt PDF.

#### T1.7 — Reserved (merged into T4.1)

#### T1.8 — §5.1: report discordant cells and the rerun; remove any accuracy-gain reading
- Priority P0 · Track S · `SUPPORTED` + `WRITING` · Depends on: T0.2 · Concerns: RC-11
- Existing evidence: §2.1 (record 5189; headroom-ablation rerun 30/30 in every arm).
- Steps:
  1. Replace `sections/results.tex` paragraph "Across the 90 held-out records … cost by 58.7\%." and the following paragraph "No held-out record is a compiled-only failure … \cref{app:config})." with Block R-1 (§5), which keeps the reductions, adds the discordant cells (baseline-only 1, compiled-only 0, exact McNemar p=1), names record 5189 and the rerun, and carries the certificate sentence from T4.1.
  2. In `appendix.tex` `app:limits` add one sentence under "A compression comparator ran, and did not engage": "That rerun also re-executed the unchanged agent on the same 30 backlog records and scored 30/30, so the single baseline miss of \cref{tab:primary} did not recur."
  3. Table 1 caption: append "The baseline column's single miss is a stochastic baseline failure (record 5189), not a compiled-only pass; see text."
  4. Abstract: "\method satisfies all 90 task requirements, compared with 89/90 for the unchanged agent" → "\method satisfies 90/90 exact task contracts and the unchanged agent 89/90, a difference of one stochastic baseline miss," (space-neutral).
- Completion criterion: §5.1 contains "baseline-only", "compiled-only", "McNemar", "5189", and "30/30" in the rerun sentence; no sentence in abstract/intro/§5.1/§8 can be read as an accuracy improvement.

#### T1.9 — Reserved (merged into T4.3)

#### T1.10 — Regenerate Figure 1 with leading-zero decimals
- Priority P2 · Track S · `WRITING` · Depends on: T0.2 · Concerns: RC-18
- Steps: in `paper/scripts/build_artifacts.py` `aha_example_figure()` (lines ~429-595) find the text `U=.0498` / `α=.05` and change to `U=0.0498 ≤ α=0.05`; run `$PY paper/scripts/build_artifacts.py`; confirm `paper/iclr/figures/gac_aha_example.pdf` hash changed and `pdftotext -f 2 -l 2 paper/iclr/build/main.pdf - | grep -c "=\.0"` → 0 after rebuild. Fix `ICLR_FIGURES` casing first (T6.3) so the copy lands in `paper/iclr/figures/`.
- Completion criterion: Figure 1 shows `U=0.0498 ≤ α=0.05`; `artifact_manifest.json` regenerated.

### Phase 2 — Reanalysis of retained data (Track S, provider-free, P1)

Implement all Phase 2 computations in **one new script**, `paper/scripts/iclr_revision_statistics.py`, with one sub-command per task (`absolute`, `paired`, `multiplicity`, `order`, `manifest`, `catalog-audit`, `overlap`, `frontier`, `clusters`, `mechanisms`, `all`). Each sub-command writes one JSON file under `paper/results/iclr_revision/` and, where a table is required, one `.tex` file under `paper/iclr/tables/` (generated tables get a first-line comment `% generated by paper/scripts/iclr_revision_statistics.py <subcommand>; do not edit`). Add `paper/scripts/test_iclr_revision_statistics.py` that pins every number quoted below to the retained files (pytest collects `paper/scripts`). Reuse `paper/scripts/github_live_study.py:bootstrap_ci` (10,000 resamples, seed 20260802) and its McNemar/Wilcoxon helpers so the new numbers use the same estimators as the existing ones. No sub-command may import the OpenAI SDK.

Input files (read-only): the three primary `results.json` (§2.1), the three cross-repository `results.json` (§2.2), `paper/results/github_workflow_families/*/headroom_ablation/results.json`, `paper/results/datasets/github_issues/train-00000-of-00001.parquet`, `paper/results/frozen_candidate_coverage_simulation.json`, `paper/results/gate_selectivity_analysis.json`.

#### T2.1 — Absolute per-record resource table (`absolute`)
- Priority P1 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-8
- Steps:
  1. For each family and arm (baseline, compiled, manual; issue-type manual = `condition == "macro"`; `repeat == 0`) compute mean and median of `requests`, `tool_calls` (interfaces), `total_tokens`, `wall_latency_ms`, `estimated_cost_usd`; also the weighted (n=90) pooled row per arm.
  2. Expected values the test must pin (mean / median): issue-type baseline 4.00/4, 3.00/3, 4259.4/4320, 6160/5402 ms, 8.72e-4/8.74e-4 USD; compiled 2.00/2, 3.00/3, 2576.5/2602.5, 2973/2881, 5.93e-4/5.74e-4; macro 2.00/2, 1.00/1, 1780.8/1734, 3251/2856, 5.45e-4/5.49e-4. PR-outcome baseline 4.00/4, 3.00/3, 2718.2/2676.5, 5329/4839, 6.79e-4/6.66e-4; compiled 1/1, 1/1, 521.5/461.5, 1440/1297, 1.68e-4/1.51e-4; manual 1/1, 1/1, 522.2/458.5, 1827/1364, 1.69e-4/1.51e-4. Backlog baseline 3.97/4, 2.97/3, 2844.9/2816.5, 6428/5163, 7.29e-4/7.10e-4; compiled 1/1, 1/1, 529.9/468, 2000/1557, 1.82e-4/1.62e-4; manual 1/1, 1/1, 535.5/479, 1695/1501, 1.88e-4/1.70e-4.
  3. Write `paper/iclr/tables/absolute_resources.tex`: rows = family × arm, columns = Requests, Interfaces, Tokens (mean / median), Latency ms (mean / median), Cost USD ×10⁻⁴ (mean / median). Add a `\begin{table}` float in `appendix.tex` `app:stats` labelled `tab:absolute-resources`, caption: "Absolute per-record resources behind the percentages of \cref{tab:primary}. Estimated dollars are list-price estimates (\cref{app:config}); the entire 90-record compiled evaluation cost an estimated \$0.028 against \$0.068 for the baseline."
  4. Add the pointer sentence to §5.1 (Block R-1 already contains it).
- Output: `paper/results/iclr_revision/absolute_resources.json`, `paper/iclr/tables/absolute_resources.tex`.
- Completion criterion: test pins pass; table compiles; `pdftotext` of Appendix D contains "4259" and "521.5".

#### T2.2 — Paired statistics upgrade (`paired`)
- Priority P1 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-8, RC-11
- Steps:
  1. For all three families and both contrasts (compiled − baseline, manual − baseline) compute: mean paired difference and 95% bootstrap CI for `total_tokens`, `wall_latency_ms`, `estimated_cost_usd`; **median** paired difference with a 95% bootstrap CI of the median for `wall_latency_ms`; two-sided Wilcoxon `p` (zero_method `zsplit`); for requests report "deterministic" (constant differences) except backlog baseline record 5189 (3 requests) — report the histogram of paired request differences instead.
  2. Exact-contract discordance per family and pooled: counts of baseline-only passes, compiled-only passes, both, neither; exact McNemar p; one-sided 95% Clopper–Pearson upper bound on the compiled-only-failure rate (pooled 0/90 → 0.0327).
  3. Replace the Appendix D table (currently two families, no issue-type row, no cost CI) with a generated `paper/iclr/tables/paired_statistics.tex`: rows = family × endpoint (tokens, latency mean, latency median, cost), columns = paired difference, 95% CI, p. Existing numbers that must reproduce: PR-outcome tokens −2196.7 [−2211.1, −2182.5], latency −3889 ms [−4499, −3346], cost −5.11e-4; backlog tokens −2315.0 [−2366.0, −2224.1], latency −4428 ms [−5400, −3558], cost −5.44e-4; issue-type tokens CI [−1772.5, −1596.7] (from `paired_test` in the replication file).
  4. Add a second small generated table `paper/iclr/tables/discordance.tex` (family, baseline-only, compiled-only, McNemar p, upper bound) and reference it from Block R-1.
- Output: `paper/results/iclr_revision/paired_statistics.json`, two `.tex` tables.
- Completion criterion: every "95% CI" cell in Appendix D contains an interval (no blank cost CI); issue-type rows present; discordance table shows 1/0 for backlog, 0/0 elsewhere, pooled 1/0, p=1.0, upper bound 3.3%.

#### T2.3 — Candidate-multiplicity accounting from retained calibration tables (`multiplicity`)
- Priority P0 · Track S · `REANALYSIS` (+ `NARROW`) · Depends on: T0.2 · Concerns: RC-1
- Steps:
  1. Parse `compiler.artifact.gate.notes` grid rows for the three primary artifacts and the eight cross-repository artifacts; recompute `U` with `gamma = delta/(m*|Lambda|)` for m ∈ {1, 2}; expected: m=1 → 0.049809; m=2 → 0.056941 at n=92, k=0.
  2. Determine the realized `m` per family **conditional on train/dev**: issue-type m=1 (`compiler.candidates[0].gate == null`, rejected at synthesis), PR-outcome m=2, backlog m=2 (`compiler.report` "candidates mined 2", dominated after calibration). Cross-repository families m=1 (`freeze_one_candidate_before_calibration true`, one mined candidate).
  3. Recover the dominated two-read candidates' calibration tables provider-free: add a flag `--retain-all-candidate-gates` to `paper/scripts/github_workflow_family_study.py` (or a thin new script `paper/scripts/recompile_family_candidates.py` that imports its `compile_artifact`/`reconstruct_discovery` and calls `compile_grc` with the identical `GrcConfig`) that serializes every `res.artifacts[*]` gate (including dominated ones) to `paper/results/iclr_revision/recompiled_candidates_<family>.json`. Inputs: `paper/results/github_workflow_families/pr_outcome/pilot_v1/discovery_checkpoint.json`, `paper/results/github_workflow_families/backlog_attention/final/discovery_checkpoint.json`. Determinism check: the emitted artifact ids must equal `cand-00-a1de3856bb6c` and `cand-00-99f1b041ed7c` and `splits.digest` must equal `46a609a3016bcf4a` / `f6a22d6c8895ee87`; otherwise stop and report.
  4. Write `paper/iclr/tables/multiplicity_accounting.tex`: rows = the three primary families; columns = candidates reaching calibration (m), n, k, U at γ=δ/|Λ|, U at γ=δ/(m|Λ|), certificate level at α=0.05 ("compiler-wide" for m=1 with U≤α; "per-candidate; compiler-wide at α≥0.057" for m=2), groups needed for compiler-wide at α=0.05 (92 / 106).
  5. Place the table in `appendix.tex` `app:config` after the "Admission budgets" paragraph and reference it from Block A-2 and Block R-1.
- Output: `paper/results/iclr_revision/multiplicity_accounting.json`, `paper/iclr/tables/multiplicity_accounting.tex`, two recompiled-candidate JSON files.
- Completion criterion: table shows issue-type m=1 U=0.0498 compiler-wide; PR-outcome and backlog m=2 U=0.0569; recompiled dominated candidates' tables retained; determinism check logged.

#### T2.4 — Condition-order and latency validity (`order`)
- Priority P1 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-12
- Steps:
  1. From `schedule[]` (pr, backlog) and `condition_order.primary.assignments` (issue-type), tabulate how many records ran each of the six arm orders (expected 5 each for pr/backlog; 10 per stage-batch design for issue-type).
  2. Compute the compiled-vs-baseline wall-latency reduction separately within each of the six permutations (5 paired records each for pr/backlog) and by position of the compiled arm (first/second/third within the record's triple). Report min and max across permutations. Also compute `provider_response_latency_ms` reductions alongside `wall_latency_ms` (expected near-identical: PR-outcome 0.7298 both).
  3. Record the cache facts: `cached_input_tokens = 0` for every pr/backlog record (no provider cache sharing between arms; `store=False`); issue-type baseline had 39,388 cached and 35,299 cache-write tokens over 30 records, compiled 20,345 / 34,959, macro 0 / 24,831 — report the cost decomposition already in `paper/results/cache_accounting.json`.
  4. Write one paragraph for `appendix.tex` `app:stats` ("Latency validity"): design (per-record interleaving over all six arm orders, arms run one at a time, one host, one region, no warm-up, 120 s timeout, no retries), the per-permutation range from step 2, and the statement that latency is wall-clock provider evidence and secondary to requests/tokens.
- Output: `paper/results/iclr_revision/order_sensitivity.json`; paragraph text in `revision_log.md` and `appendix.tex`.
- Completion criterion: the paragraph states the six-permutation design and the min–max reduction range; every number has a key path in the JSON.

#### T2.5 — Live-provider reproducibility manifest (`manifest`)
- Priority P1 · Track S · `SUPPORTED` + `WRITING` · Depends on: T0.2 · Concerns: RC-9
- Steps: generate `paper/iclr/tables/live_provider_manifest.tex` (Appendix C, two-column key/value) from the `run` blocks and the code constants below; each row's source is recorded in the JSON:

  | Key | Value (source) |
  |---|---|
  | Provider / model string | OpenAI `gpt-5.6-luna` via `openai-agents` Responses interface (`run.model`) |
  | SDK versions | `openai-agents 0.19.2`, `openai 2.52.0`, Python 3.14.4, macOS 26.5.2 arm64 (`run.openai_agents_sdk`, `run.openai_python`, `run.python`, `run.platform`) |
  | Model settings | reasoning effort `low`, verbosity `low`, `parallel_tool_calls=False`, `store=False` (`paper/scripts/github_live_study.py:522-530`) |
  | Sampling | temperature / top-p / seed not settable for this model; SDK sends none (`run.temperature` in the issue-type file) |
  | Turn and time limits | `max_turns=8` (families), `6` (cross-repository); `asyncio.wait_for(..., 120.0)` per episode; no retries; failed episodes recorded in `failures[]` |
  | Concurrency | discovery 6 (pr, backlog) / 8 (issue-type, cross-repository); held-out arms one at a time per record (pr, backlog, cross-repository); blocked six-permutation Latin design with 8-way concurrency (issue-type) |
  | Condition order | per-record permutation cycle over all six arm orders (`schedule[]`) |
  | Cache accounting | token fields `input_tokens`, `input_tokens_details.cached_tokens`, `input_tokens_details.cache_write_tokens`, `output_tokens`; arms share no server-side state (`store=False`); observed cached tokens: 0 in pr/backlog/cross-repository, non-zero in issue-type (values in `cache_accounting.json`) |
  | Pricing | USD per 1M tokens: input 0.20, cached input 0.02, cache write 0.25, output 1.20, list rates retrieved 2026-08-02 (`demos/live_runtime.py:58-65`); cost = (input − cached − cache_write)·0.20 + cached·0.02 + cache_write·0.25 + output·1.20 |
  | Data pins | `helmo/github-issues@e344be7b…` parquet sha256 `09453eef…` (tracked); four cross-repository parquets pinned by sha256 in `paper/results/datasets/github_multirepo/*/source_manifest.json` (revision `main`, content fixed by digest; files gitignored, re-downloadable) |
  | Tool/effect pins | per-family `effect_catalog_version` digests, `tools_hash`, `prompt_hash`, `policy_hash`, `entry_contract_version` (from `compiler.artifact.guard` manifest pins) |
  | Timestamps | one `run.timestamp_utc` per study (2026-08-04/05/07/22); per-record timestamps not retained |

  Replace the sentence "Sampling parameters are not overridden and take provider defaults." (`appendix.tex:112-113`) with "Sampling parameters are not exposed for this model; the SDK sends none, and the effective request manifest is \cref{tab:live-manifest}." Add Block REPRO-1 (§5) to the Reproducibility Statement in `main.tex`.
- Output: `paper/results/iclr_revision/live_provider_manifest.json`, `paper/iclr/tables/live_provider_manifest.tex`.
- Completion criterion: Appendix C contains the manifest table; the phrase "provider defaults" no longer appears; the Reproducibility Statement lists snapshot, SDK versions, settings, cache policy, timeout/retry policy, order, concurrency, token fields, rate date, catalog hashes, and split manifests.

#### T2.6 — Live effect-catalog trust root and empirical audit (`catalog-audit`)
- Priority P1 · Track S · `SUPPORTED` + `WRITING` · Depends on: T0.2 · Concerns: RC-13
- Steps:
  1. Import `make_catalog()` from the three study scripts (`github_workflow_family_study.py:506`, `github_live_study.py`, `github_multirepo_pr_outcome_core.py`) and serialize per tool: effect class, capabilities, `quota_attested`, `replayable`, `speculatable`, isolation partition, `catalog_version` digest (must match the digests in the artifact manifests, e.g. `github-pr_outcome-pinned-reads@1#821a5aef76e5ebf1`).
  2. Empirical audit over every retained trace (discovery, held-out, repeats, headroom reruns, cross-repository): count calls per tool name; assert every name is in the declared read-only vocabulary (`quality.tool_contract` is true on 100% of retained records — verify and report the count); state that every tool is a pure function of the pinned parquet (`execute_snapshot`), performs no network or write operation, and that hidden-state/RNG checks are therefore vacuous by construction rather than unavailable.
  3. Write a short generated table `paper/iclr/tables/live_catalog.tex` (tool, effect, capabilities, calls observed) and a paragraph for `appendix.tex` `app:external` or `app:limits` titled "Trust root of the live GitHub catalog": who owns the declaration (the study author, signed into the catalog digest), what invalidates an artifact (any catalog digest change is a manifest mismatch and a dispatch barrier, `EffectCatalog.matches_version`), and the audit counts.
  4. Make "an unknown effect is not a read" visible in `figures/pipeline_overview.tex` barrier band: change "unknown or irreversible effect" to "unknown effect (never a read) or irreversible effect".
- Output: `paper/results/iclr_revision/live_catalog_audit.json`, `paper/iclr/tables/live_catalog.tex`.
- Completion criterion: paragraph and table present; the barrier band contains "never a read"; audit JSON reports 100% tool-contract compliance with total call counts.

#### T2.7 — Cohort-overlap audit (`overlap`)
- Priority P1 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-14
- Steps: compute pairwise overlaps of discovery, calibration, and held-out identifiers across core, balanced, and gate-frontier per repository (balanced uses `issue_number`; others `record_number`). Expected: core ∩ gate-frontier test 130/150 pooled (23, 27, 23, 28, 29 per repo), calibration 442/460, discovery 561/580; core ∩ balanced test 13/30 (pandas), 25/30 (psf), 8/30 (pytorch); balanced ∩ gate-frontier test 28/60, 52/60, 17/60. Write the JSON and feed the numbers to Block F1-1 and Block H-1.
- Output: `paper/results/iclr_revision/cohort_overlap_audit.json`.
- Completion criterion: JSON matches the expected counts; the word "independent" is used for cohorts only where the JSON shows zero overlap (none of these).

#### T2.8 — Gate-frontier and cross-repository reanalysis (`frontier`)
- Priority P0 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-2, RC-6, RC-22, RC-23
- Steps:
  1. Per arm and repository: attempted, completed, exact passes (ITT and completed-case); pooled: baseline 240/240, learned 240/240, support-only 239/240; identify the failure (`failures[0]`).
  2. Held-out dispatch coverage: count `dispatch.outcome == "COMPACTED"` vs `"BASELINE"` per repository and arm, with `dispatch.reasons` histogram (expected `["range:pr.state"]` for all 80 fallbacks; all 20 per repo are `class == open`). Same for core (20/30 per repo, 80/120 pooled) and balanced (60/60, 180/180).
  3. Verify the sweep rows (`coverage_curves`) and compute the support-only U from the 12-point grid (0.0507); confirm huggingface's 10/92 point (U=0.375) and balanced pandas' 90/92 point (U=0.0509) as "observed, not certifiable" intermediate coverages.
  4. `dispatch.q` constancy per repository (0.0169 hf; 0.0815 others; balanced pandas 0.2306) and the feature spec (`hull_kinds {"z.record_number": "any"}`) → the sentence "entry state is one integer, so q is constant within a repository".
  5. Sum retained `estimated_cost_usd` for the gate-frontier study (expected $0.4058) for the protocol correction in T1.1.
  6. Regenerate `paper/iclr/tables/gate_frontier_disaggregated.tex` from this JSON (T1.1 specifies the layout) and add `paper/iclr/tables/multirepo_dispatch.tex` (protocol × repository: held-out, dispatched, fallback reason, requests reduction) for Appendix F.1.
- Output: `paper/results/iclr_revision/gate_frontier_reanalysis.json`, two tables.
- Completion criterion: Table 10 rows regenerate from JSON; Appendix F.1 shows 80/120 and 180/180 dispatch; §5.2 text (Block F1-1) cites the 80/120 figure.

#### T2.9 — Effective independent units for the i.i.d. assumption (`clusters`)
- Priority P1 · Track S · `REANALYSIS` · Depends on: T0.2 · Concerns: RC-10
- Steps:
  1. For each primary family, join the 92 calibration group ids (`compiler.splits.groups.calibration`) to the pinned parquet rows (issue/PR number → `created_at` day, author login, and for PRs `merged_at` day).
  2. Compute n_eff(day) = distinct creation days, n_eff(author) = distinct authors, n_eff(month) = distinct months; and the conservative cluster-level bound U(n_eff, k=0) at γ=δ/|Λ| for each (admits iff n_eff ≥ 92).
  3. Repeat for the four completed core repositories if the gitignored parquets can be re-downloaded and their sha256 matches `source_manifest.json`; otherwise report `huggingface/datasets` only and say so.
  4. Write `paper/iclr/tables/effective_units.tex` (family/repo, n, distinct days, distinct authors, U at n_eff(day), U at n_eff(author)) and one sentence for §7 and one paragraph for `appendix.tex` `app:limits` "Repairs for non-exchangeable groups": "\Cref{tab:effective-units} reports the effective unit counts the live cohorts actually contain; where a cluster-level count falls below 92 the corresponding conservative bound retires, which quantifies the exposure \cref{sec:discussion} describes."
- Output: `paper/results/iclr_revision/effective_units.json`, `paper/iclr/tables/effective_units.tex`.
- Completion criterion: table present with real counts; §7 states the day- and author-level counts for at least the three primary families; no claim of "distribution-free" anywhere.

#### T2.10 — Mechanism-removal table from retained failure cases (`mechanisms`)
- Priority P1 · Track S · `SUPPORTED` · Depends on: T0.2 · Concerns: RC-7
- Steps: generate `paper/iclr/tables/mechanism_removal.tex` with one row per retained hazard, columns: hazard, where observed (study, record), guard that caught it (stage of Alg. 1), what recurrence-only replay would have done. Rows and sources:

  | Hazard | Observed | Guard | Recurrence-only outcome |
  |---|---|---|---|
  | Ungrounded argument `issue_get_comments.limit=100` recurring in 132/132 discovery traces | issue-type, `compiler.candidates[0]` in `github_natural_replication/results.json` | Stage 1/4 provenance + synthesis (`ungroundable_slot`) | replays the literal; emits the three-read program |
  | Clean tool replay, wrong downstream answer (Markdown URL dropped, issue 6602) | `github_natural_live/continuation_replay.json`, 17/18 → 18/18 after checked rendering | continuation contract (post-model check) | accepts the answer |
  | Getter-named method that mutates state on 36/36 calls | BFCL executed gold plans, `external_benchmarks/bfcl_compiler_execution.json` | signed effect declaration (Stage 2 qualification) | compiles a write into the region |
  | API-documentation prologue before the hot prefix | AppWorld released runs, 4,679/4,680 ReAct and plan-and-execute trajectories, `appworld_dispatch_preflight.json` | position invariant Eq. (2) (dispatch) | dispatches out of position |
  | Suffix region dispatched at entry: reordered and duplicated calls | archived pilot `github_live/pilot_2026-08-03/`, 16.7% pass | position invariant (compile-time `prefix_only`) | ships the fault |
  | Result outside the induced hull (`pr.state = open`; 40/120 core held-out records) | `github_multirepo_pr_outcome_core/results.json` `dispatch.reasons` | induced verifier V, clean fallback | answers from an unseen class without a check |
  | Projection type mismatch (`merged_at`, record 7199; 13 events) | `pr_outcome/pilot_v1/failed_attempt_projection_2026-08-05.json` | composite projection verifier, fallback | emits a malformed evidence object |
  | Recurrent but under-supported families (n_max 26, 8, 15 < 92) | NESTFUL, API-Bank, BFCL | Stage 6 exact gate | deploys on recurrence alone |
  | Skewed calibration pool (84/30/2 classes) | `pytorch/pytorch` core, `repository_failures[0]` | Stage 6 exact gate (U=1.0) | deploys |

  Place the table in `appendix.tex` `app:limits` directly after the paragraph "Why the workflow-reuse line is not a drop-in comparator", with the lead sentence from Block RW-1. Every cell must carry the file path in a footnote or the caption.
- Output: `paper/results/iclr_revision/mechanism_removal.json`, `paper/iclr/tables/mechanism_removal.tex`.
- Completion criterion: nine rows, each with a retained-file source; the §7 sentence "leaving the unguarded ablation unrun" is revised per T4.2 to point at this table and at the pre-registered live arm (T3.2).

#### T2.11 — Recover the pytorch retirement's per-threshold table (provider-free recompile)
- Priority P2 · Track S (promotable to R) · `REANALYSIS` · Depends on: T0.2 · Concerns: docx §2.1 "pytorch retirement mechanism" (RESOLVED but under-evidenced)
- Issue: Appendix F.1 attributes the retirement to class composition; the retained error string is truncated at 120 characters (`grc/compile.py:512` stores `gate.notes[:120]`) and no per-η n/k/U rows exist, so "no group admitted at any η" vs "every admitted group violated" cannot be distinguished.
- Steps:
  1. Change `grc/compile.py:512` to store the full `gate.notes` (no slice) and, in `grc/calibrate.py:424-437`, include the grid rows in the retired `Gate.notes` exactly as for admitted gates. Add a unit test.
  2. Recompile `pytorch/pytorch` provider-free from `paper/results/github_multirepo_pr_outcome_core/repos/pytorch__pytorch/discovery_checkpoint.json` using `reconstruct_discovery` and the identical `GrcConfig` (seed 20260807, freeze on); write `paper/results/iclr_revision/pytorch_core_recompile.json` with the full grid rows.
  3. Update the Appendix F.1 paragraph to state the identified mechanism (n_η = 0 at every η because the constant q exceeded every grid threshold, or k_η = n_η, whichever the rows show) and keep the class-composition explanation only if the rows support it.
- Completion criterion: JSON contains 11 rows for the retired candidate; Appendix F.1 mechanism sentence cites them; test passes.

### Phase 3 — New experiments and code changes

Every experiment here has a protocol file committed **before** any provider call, an explicit spend ceiling, a pre-declared decision rule, and a results directory that does not overwrite anything.

#### T3.1 — Multiplicity repair: fresh calibration cohorts for PR-outcome and backlog (Track A)
- Priority P0 · Track R (promotable) · `NEW-EXP` · Depends on: T2.3 · Concerns: RC-1
- Hypothesis: with both mined candidates frozen (P, H, V, q from the retained registries) and m=2 Bonferroni (γ = δ/22), a fresh class-balanced cohort of 106 calibration groups per family yields zero violations for the dominant candidate, giving U ≤ 0.0496 and a compiler-wide certificate at α=0.05 for the existing artifacts. Issue-type needs no repair (m=1).
- Setup:
  1. Protocol file `paper/supplementary/multiplicity-repair-protocol.md` committed first: candidate ids (`cand-00-a1de3856bb6c` + its dominated two-read sibling from T2.3; `cand-00-99f1b041ed7c` + sibling), registries (`paper/results/github_workflow_families/{pr_outcome,backlog_attention}/final/registry/registry.json`), α=0.05, δ=0.1, Λ (11 points), m=2, n=106 per family, class-balanced draw (36/35/35 by round-robin over the class order used in `select_cases`), seed 20260921, exclusion of every record already used in any retained pr_outcome / backlog cohort (discovery, held-out, pilot, headroom), timeout 120 s, **retry rule: one retry per timed-out episode, both attempts retained**, violation label = the same replay-contract label `calibrate_gate` consumes today, stopping rule: n fixed at 106 before any provider call, decision rule below.
  2. Preflight (provider-free): `paper/scripts/multiplicity_repair_study.py --family pr_outcome --preflight-only` counts unused eligible records per class from the pinned parquet after exclusions (pr_outcome pool before the final selection was 553 / 3,567 / 43 for closed_unmerged / merged / open). Go/no-go: proceed only if every class has ≥ 36 unused records; otherwise record NO-GO and use Track B wording.
  3. Run: 106 baseline discovery episodes per family (the unchanged agent, same prompt, same tools) → `paper/results/multiplicity_repair/<family>/discovery_checkpoint.json`; then provider-free calibration of **both** frozen candidates on the 106 groups with `candidate_multiplicity=2` (new keyword on `calibrate_gate`, `conf = 1 - delta/(m*len(grid))`, default `m=1`, unit-tested) → `results.json` with the full grid rows for both candidates.
  4. Spend ceiling: `--approved-spend-usd 0.50` per family (expected ≈ $0.09 per family at the observed $0.0007–0.0009 per baseline episode).
- Independent variables: family; candidate (2). Dependent: n_η, k_η, U_η per η for each candidate; admit/retire under γ=δ/22.
- Decision rule (pre-declared): (i) both candidates zero violations → compiler-wide certificate at α=0.05 for the dominance-selected artifact; use Track A language. (ii) any violation → report the corrected U; if U > α the family's compiler-wide claim is not made (Track B language stays) and the violation record is described. (iii) NO-GO at preflight → Track B. Never change α, δ, Λ, m, or n after the run.
- Output artifacts: protocol file; `paper/results/multiplicity_repair/{pr_outcome,backlog_attention}/{preflight.json,discovery_checkpoint.json,results.json}`; generated `paper/iclr/tables/multiplicity_repair.tex`; validator check pinning the numbers.
- Paper location: Appendix C (table), Appendix A Block A-2 (certificate statement), §5.1 Block R-1, abstract (Track A sentence), §7 opening.
- Completion criterion: results committed with both candidates' full grid rows; abstract/§3/§5.1/§7/App A/App C use one certificate track consistently; if retired, the retirement sentence appears in §5.1 and §7.

#### T3.2 — Recurrence-only replay arm (B1) on the issue-type family
- Priority P1 · Track R (promotable) · `NEW-EXP` · Depends on: T2.10 · Concerns: RC-7
- Why issue-type only: on PR-outcome and backlog the mined three-read sequence **is** the compiled program (both candidates reached calibration; the deeper one was admitted), so a recurrence-only replay executes the identical calls and differs only in the absence of H/V/q — which never fired on in-distribution held-out records (dispatch 30/30). A live B1 there would reproduce the compiled arm by construction; state this in the paper instead of spending. On issue-type the compiler refused the three-read candidate (`limit` ungroundable) and emitted the two-read prefix, so B1 differs.
- Hypotheses (pre-declared, either outcome reported): H-B1a: the unguarded three-read replay (record → labels → comments with the literal `limit=100`) followed by one model request preserves exact contracts on the 30 sealed held-out records (30/30). H-B1b: it saves one further request per record relative to the compiled two-read artifact (1 vs 2). H-B1c (continuation): at least one of the 30 answers loses excerpt/URL exactness the way issue 6602 did.
- Setup: `paper/scripts/recurrence_only_ablation.py`: load the sealed 30-record selection from `paper/results/github_natural_replication/results.json` (`selection.test`), build a `ManualPreModelPlan` with three `CallStep`s (`issue_get_record`, `issue_get_labels`, `issue_get_comments(limit=Const(100))`), permissive `Verifier()` (no clauses; asserted inert), no gate, manifest pins identical to the retained artifact; run the arm live once per record with the same model settings, `max_turns=8`, `timeout=120`, one retry on timeout (both retained); grade with the retained `natural-extractive-v2` exact oracle (`github_live_study.py --regrade-results` path). Spend ceiling `--approved-spend-usd 0.25` (expected ≈ $0.02).
- Comparators: retained baseline, compiled, macro arms on the same 30 records (paired).
- Metrics: exact contract (factuality_exact), per-field exactness, requests, interfaces, tokens, latency, cost; discordant cells vs compiled (McNemar); 95% bootstrap CIs on paired differences.
- Decision rule: if B1 passes 30/30, report that on this cohort the provenance refusal cost one request per record without a measured quality benefit and that the guard's value is the refusal of an unwitnessed argument plus the retained 6602 counterexample (18-record cohort), not a held-out failure; if B1 fails ≥ 1, report the failure record and mechanism as the direct price of the missing guard. Never rerun to change the count.
- Output: `paper/supplementary/recurrence-only-ablation-protocol.md` (before the run), `paper/results/recurrence_only_ablation/{preflight.json,results.json}`, generated `paper/iclr/tables/recurrence_only.tex` (arm × metric, 30 records), validator check.
- Paper location: Appendix G after the mechanism-removal table; one sentence in §7 (Block D-1) and §6 (Block RW-1).
- Completion criterion: results committed with 30 graded records (plus any retry pairs), table generated, §7 sentence states the outcome in the same words as the decision rule.

#### T3.3 — Drift-robustness ablation (pre-registered protocol, provider-free)
- Priority P2 · Track R (promotable) · `NEW-EXP` (no spend) · Depends on: T0.2 · Concerns: RC-7 (arms B2–B4 separate only under shift)
- Setup: implement exactly `paper/supplementary/drift-robustness-ablation-protocol.md` (three arms `compiled_guarded`, `compiled_unverified`, `manual_unverified`; five demo substrates; nine perturbation families; `max_windows=24`; McNemar with Holm over two comparisons; Clopper–Pearson upper bounds on zero counts; over-abstention guardrail 0.25/0.50). Driver `paper/scripts/drift_ablation_study.py` writing `paper/results/drift_ablation/results.json`; permissive-verifier inertness test; validator check.
- Decision rule: as written in the protocol (four readings). A null is committed as a null.
- Paper location: Appendix G paragraph "A drift-parity protocol, pre-registered and not yet run" → replace "Status: … not executed" with the outcome and the per-family wrong counts; if positive, add one sentence to §7.
- Completion criterion: results committed; protocol status updated; the Appendix G paragraph states counts per arm.

#### T3.4 — Larger calibration pool for a graded frontier (Track F: protocol only)
- Priority P2 · Track F · `NEW-EXP` · Concerns: RC-17
- Write `paper/supplementary/graded-frontier-protocol.md`: |K| ≥ 184 groups per repository (certifies c = 0.5); an entry state with informative features (record age, discussion length, label ambiguity) rather than a bare record number; the Markdown-link stratification the pilot identified; ≥ 3 target coverage levels with the arithmetic table of §0.6; decision rule; spend estimate (≈ 3× the gate-frontier study). Do not run. Cite from Appendix H as "the protocol that could answer the question this appendix could not".
- Completion criterion: file exists and Appendix H cites it.

#### T3.5 — Read-only prologue for ReAct dispatchability (Track F: protocol only)
- Priority P2 · Track F · `NEW-EXP` · Concerns: RC-16
- Write `paper/supplementary/read-only-prologue-protocol.md` with the acceptance tests from the docx §4.3 (prologue mismatch/extra call/order change/manifest mismatch/unknown effect → unchanged baseline; no committed call replayed; provenance recomputed with prologue outputs as live-ins; pilot duplicate/reorder as regression; AppWorld eligibility recomputed per architecture). Do not implement. Appendix G keeps "neither built nor tested".
- Completion criterion: file exists; Appendix G cites it.

#### T3.6 — Enforce the position invariant at runtime (code change, provider-free)
- Priority P0 · Track S · code · Depends on: T0.2; ships with T1.5 · Concerns: RC-4
- Steps:
  1. In `src/guarded_agentic_compaction/runtime/dispatch.py:264-268`, before the tool-intersection test, add: if `art.program is not None and len(already_observed) > 0` → fallback with reason `"non_prefix_boundary"`. Keep the intersection test (now redundant) for defense in depth. Apply the same rule in `runtime/manual.py:127-128` for pre-model plans.
  2. Add unit tests: (a) an unrelated prior tool observation → `DispatchOutcome.BASELINE` with reason `non_prefix_boundary`; (b) empty `already_observed` → unchanged behavior; (c) `model_provider._observed_tools` on an SDK history containing one `function_call` item returns a non-empty set.
  3. Run the full suite. If any existing test dispatched a GRC artifact after an unrelated prior call, do not weaken the rule; report the test and stop.
  4. Add a `revision_log.md` entry and mention in Appendix C Block C-1 that position 0 is enforced as "no prior tool observation".
- Completion criterion: new tests pass; full suite green; Algorithm 4 line matches the code.

#### T3.7 — Retain full retire notes (code change, ships with T2.11)
- Covered in T2.11 steps 1–2.

### Phase 4 — Claim-hierarchy rewrite (Track S, writing; Track A variants gated on T3.1)

#### T4.1 — One certificate level everywhere
- Priority P0 · Track S · `WRITING` (+ `NARROW` for two families) · Depends on: T2.3 (numbers), T1.3 · Concerns: RC-1
- Default is **Track B** (no new evidence). If T3.1 completes with both families admitting under γ=δ/22, switch every Track B block to its Track A twin in one commit.
- Steps:
  1. Abstract: replace "Those admission certificates are per candidate; extending them compiler-wide requires freezing the candidate search." with Block ABS-B (or ABS-A).
  2. §3 after Proposition 1: replace the paragraph "\Cref{app:proof} gives the proof. The guarantee is deliberately narrow: …" with Block M-1.
  3. §5.1: Block R-1 (contains the Track B/A certificate sentence).
  4. §7 opening: Block D-1 replaces the first paragraph of "Gate maturity is the main scientific risk" up to "The follow-up is to buy calibration groups first".
  5. Appendix A: Blocks A-1, A-2 (T1.3). Appendix C: multiplicity table (T2.3) and, under Track A, the repair table (T3.1).
  6. Introduction, contribution 1: after "finite-sample compile-or-retire admission (\cref{alg:calibrate}, \cref{prop:admission})" insert "with the certificate level stated per artifact"; no other intro change.
  7. Conclusion: no change needed unless Track A ("…preserves exact held-out contracts under compiler-wide admission…" is allowed only under Track A).
- Completion criterion: `grep -n -i "per-candidate\|compiler-wide\|per candidate" paper/iclr/sections/*.tex paper/iclr/appendix.tex` lists only sentences consistent with the chosen track; the revision log records the track choice and the T3.1 outcome that justified it.

#### T4.2 — Comparator positioning and Headroom scoping
- Priority P1 · Track S · `WRITING` · Depends on: T2.10 · Concerns: RC-15, RC-7
- Steps:
  1. §6 "Guarded trace and agent workflow compilation" paragraph: after "…each retain a veto." append one sentence: "We price those vetoes internally (\cref{tab:mechanism-removal}) rather than against reuse systems that make no admissibility decision." Remove nothing else in §6 unless T4.6 requires it.
  2. §7 "External validity, dispatchability, and comparison": replace "…and run no head-to-head against the workflow-reuse line, whose prompt-level induction, parallel scheduling, and plan caching make no admissibility decision to compare against --- leaving the \emph{unguarded} ablation unrun." with Block RW-1's §7 sentence.
  3. Appendix G "Why the workflow-reuse line is not a drop-in comparator": keep; replace its last two sentences ("The comparison we owe and did not run is instead internal … single highest-value addition") with Block RW-1's appendix sentences and the mechanism-removal table float.
  4. Appendix G "A compression comparator ran, and did not engage": add the closing sentence "It is a compression comparator only; it does not stand in for a workflow-reuse or unguarded-replay baseline."
- Completion criterion: the phrase "unguarded ablation unrun" is gone; Appendix G contains the mechanism-removal table and the recurrence-only arm's status (pre-registered or reported).

#### T4.3 — Gate interpretation: support threshold, not a learned-gate verdict
- Priority P0 · Track S · `WRITING` · Depends on: T2.8 · Concerns: RC-6
- Steps:
  1. §3 Stage 6 second paragraph ("That floor also bounds what selectivity is certifiable…") — keep; append: "It also means these studies test support sufficiency at full coverage and cannot identify whether $q$ ranks dispatch risk."
  2. §7 "Gate maturity" paragraph: Block D-1 (T4.1) already carries the corrected reading; ensure the sentence about the prospective protocol reads "…a support-only comparator sharing every mechanism but the risk budget is indistinguishable from the learned gate — as it must be at $|\mathcal{K}|=92$, where neither can certify an intermediate coverage, and where $q$ is constant within a repository because the entry state is a single record number."
  3. Appendix C "What cohort size permits the gate to certify": append the two observed-but-uncertifiable points: huggingface/datasets 10/92 (U=0.375) in the gate-frontier study and pandas-dev/pandas 90/92 (U=0.0509) in the balanced rerun.
  4. Appendix F.4: rename to "The registered gate is a support threshold" and change the first sentence to "…the registered gate behaves as, and at $|\mathcal{K}|=92$ can only behave as, a support threshold; the question of $q$'s ranking quality is open, not answered negatively." Table 9 caption: "Gate-behavior inventory …".
  5. Appendix H: Block H-1 (T1.1).
- Completion criterion: no sentence in the paper attributes the step profile to q's inability to rank; every mention of the step profile names the |K| arithmetic.

#### T4.4 — i.i.d. qualifier placement
- Priority P1 · Track S · `WRITING` · Depends on: T2.9 · Concerns: RC-10
- Steps: the qualifier "under i.i.d. calibration groups" (or "conditional on i.i.d. groups") must appear in: abstract (ABS block), §3 (M-1), §5.1 (R-1), §7 (D-1), Appendix A (already), Appendix C admission-budget paragraph (add). Add the effective-unit counts from T2.9 to D-1 and to Appendix G "Repairs for non-exchangeable groups". Confirm `grep -rn -i "distribution-free" paper/iclr` → none.
- Completion criterion: six locations carry the qualifier; T2.9 numbers appear in §7 and Appendix G.

#### T4.5 — §5.2 and Appendix F.1: dispatch coverage, shared pools, completeness gate
- Priority P0 · Track S · `WRITING` · Depends on: T2.7, T2.8 · Concerns: RC-22, RC-14
- Steps:
  1. §5.2: replace the paragraph body with Block F1-1's §5.2 text (space-neutral: the pytorch mechanism sentence is shortened to a pointer, and the dispatch fact is added).
  2. Appendix F.1: insert Block F1-1's appendix paragraph before the disaggregated table; add the generated `paper/iclr/tables/multirepo_dispatch.tex` float (`tab:multirepo-dispatch`).
  3. Appendix F.1 after the disaggregated table: replace "These rows show breadth across repositories, not independent evidence for an average effect" with "These rows show breadth across repositories drawn from shared pools — the balanced rerun shares 13, 25, and 8 of its 30-record core held-out sets — not independent evidence for an average effect".
  4. Add the completeness-gate disclosure sentence (in Block F1-1).
  5. Fix `paper/supplementary/github-multirepo-pr-outcome-core-summary.md` ("300-case cohort (60 per repository)" → "150-case capacity (30 per repository) in the executed core run").
- Completion criterion: §5.2 states 80/120 and 180/180 dispatch; Appendix F.1 contains the dispatch table and the disclosure; "independent" no longer describes any multirepo cohort relation.

#### T4.6 — Nine-page budget ledger
- Priority P0 · Track S · process · Depends on: all Phase 4 edits · Concerns: RC-19
- Main-text insertions in this plan: Block R-1 (+2 lines net), Block M-1 (+1), Block D-1 (+3), §5.2 (0), §6 (+1), §3 Stage 6 (+1), abstract (0), contribution 1 (+0.5). Budget ≈ +9 lines. Pre-approved cuts, apply in this order until the conclusion ends on page 9:
  1. §5.3 final paragraph: delete "and the architecture explains the spread: $2{,}339/2{,}340$ for full code with reflection and $762/1{,}170$ for iterative parallel function calling, against $1$ of $4{,}680$ for ReAct and plan-and-execute, which open by reading API documentation so the prefix leaves position~0" → "and the architecture explains the spread (\cref{tab:appworld-dispatch}): full-code agents are eligible almost always, ReAct and plan-and-execute almost never, because they open by reading API documentation." (−3 lines; the numbers remain in Table 7 and Appendix F.3.)
  2. Introduction contribution 4: "the first external corpus whose sample size makes admission reachable, and which admits" → "which admits" (−1 line).
  3. §6 second paragraph: delete "NESTFUL and API-Bank retain observed intermediate results directly; BFCL and AppWorld become compiler substrates only once gold artifacts run on their pinned backends, and no provider-free row measures agent quality." (−3 lines; stated in Appendix F.3.)
  4. §2 "This is not aesthetic. A suffix program dispatched at entry can reorder or duplicate calls the agent has already made --- a semantic mismatch between compilation-time and resolution-time position rather than a tuning failure, and a pilot that violated \cref{eq:prefix} produced exactly that fault (\cref{app:pilot})." → "A suffix program dispatched at entry can reorder or duplicate calls the agent has already made; a pilot that violated \cref{eq:prefix} produced exactly that fault (\cref{app:pilot})." (−2 lines.)
  5. §4 "Provider configuration" paragraph: "and price tokens from published list rates rather than an invoice" → "priced at list rates" (−1 line).
- Verification after each cut: rebuild; `pdftotext -f 9 -l 9 … | tail -20` shows the conclusion's last line; `pdftotext -f 10 -l 10 … | head -5` starts with the AI Use Statement heading; `validate_iclr_page_budget` passes.
- Completion criterion: conclusion ends on page 9; no float is orphaned onto page 10; all cut sentences' facts remain in an appendix or table.

#### T4.7 — OpenReview metadata alignment (user action, text supplied)
- Priority P1 · Track S · `WRITING` · Depends on: T4.1
- Deliver in `paper/iclr/notes/openreview_metadata.md`: the final abstract text (identical to `sections/abstract.tex` rendered), the TL;DR (Block TLDR), and keywords. The user pastes them; the executor cannot access OpenReview.
- Completion criterion: file exists and its abstract equals the compiled abstract (`pdftotext -f 1 -l 1` normalized compare).

### Phase 5 — Paper-wide consistency pass (Track S, after Phase 4)

#### T5.1 — Claim consistency grid
For each claim, grep every surface and record the line numbers in `revision_log.md`. All surfaces must agree with the chosen track.

| Claim | Surfaces to check | Grep |
|---|---|---|
| Certificate level (per-candidate vs compiler-wide; m per family; 0.0569; 106) | abstract, §1 contributions, §3 Stage 6 + after Prop. 1, §5.1, §5.2, §7, App A, App C, Reproducibility Statement, conclusion | `grep -n -i "certificate\|compiler-wide\|per-candidate\|0\.0569\|106" paper/iclr/sections/*.tex paper/iclr/appendix.tex paper/iclr/main.tex` |
| 90/90 vs 89/90 reading | abstract, §5.1, Table 1 caption, App D, App G, conclusion | `grep -n "89/90\|90/90" …` |
| Gate step profile ↔ \|K\| arithmetic; q constant | §3, §5.3, §7, App C, App F.4, App H, Table 9, Table 10 | `grep -n -i "step\|frontier\|graded\|rank" …` |
| Gate-frontier accounting (240/240, 240/240, 239/240; 160/240 dispatch; psf reuse; 2×) | §7, App H, Table 10, protocol .md, body.tex, evidence-register.md | `grep -rn "239\|240" paper/iclr paper/tex/body.tex paper/supplementary` |
| Cross-repository dispatch (80/120; 180/180) and cohort overlap wording | §5.2, App F.1, App H, body.tex | `grep -rn -i "independent\|120/120\|180/180" …` |
| i.i.d. qualifier present; no "distribution-free" | abstract, §3, §5.1, §7, App A, App C | see T4.4 |
| Numbers with sources | every numeral in §1–§8 | build the registry in T5.2 |
| Figure/table/algorithm references resolve | all | `grep -c "??" <(pdftotext paper/iclr/build/main.pdf -)` → 0; ICLR build log has no "undefined" (build with `--keep-logs` once for this check) |
| Anonymity | PDF page 1, metadata | commands in §0.2 |

#### T5.2 — Number registry
Create `paper/iclr/notes/number_registry.md`: one row per numeral appearing in `sections/*.tex` and `appendix.tex` (excluding equation constants and citations), with the JSON key path or script that produces it. Any number without a source is a defect: fix the text or add the source. Minimum rows: 90/90, 89/90, 66.6, 63.1, 64.2, 58.7, per-family reductions (15 numbers), 92, 0.0498, 0.0503, 0.0569, 106, 132, 30, 116, 580/580, 120/120, 180/180, 80/120, 44.4, 52.4, 49.4, 48.6, 66.7, 78.4, 60.7, 72.7, 240, 239, 160/240, 0.1087, 0.375, 0.0507, 1,415, 212, 200, 1,142, 136/147, 7,070, 26, 8, 15, 8,190, 2,339/2,340, 762/1,170, 1/4,680, 37.9, 3,752, 61, 0.377, 0.380, 23 (operators), κ=3, r*=0.6365, 0.00909, 1.8, 13.6, 56, 20260822, 200,000, 3.3, 9.5.

#### T5.3 — Cross-manuscript mirror
The long-form article shares claims and the validator reads it. Apply the same factual corrections to `paper/tex/body.tex` at: 951–967 (multiplicity simulation wording), 944–946 (frozen-search statement), 1403–1445 and 1928–1968 (cross-repository dispatch coverage and overlap wording), 2857–2875 (gate-frontier accounting); and to `paper/supplementary/evidence-register.md:24-25`, `paper/supplementary/review-score-90-plus-plan.md` (append a dated correction note; do not rewrite history), `paper/scripts/refresh_gate_frontier_slide.py` prose strings ("four repositories, 240 held-out pairs" stays; add "239/240 support-only"), and `site/` only via `scripts/build_pages.py`. Keep validator phrase checks satisfied (`validate_claim_boundaries` requires `"Per-candidate selective-risk admission"`, `"not a multiplicity-corrected compiler-wide certificate"`, `"92\s+to\s+106\s+admitted groups"` in body.tex; do not delete them).
- Completion criterion: `validate_artifacts.py` passes; `grep -rn "239/239\|five-times\|four times the prior" paper/ site/ docs/` → none.

#### T5.4 — Final hostile read (two passes, one statistical, one systems)
Two independent read-throughs of the rebuilt PDF by a fresh agent instance each, with a single question: pass 1 "is any guarantee stated at a level the evidence does not support?"; pass 2 "does any algorithm line, runtime claim, or effect statement disagree with the code facts in §2.4?". Each pass writes findings to `revision_log.md`; every finding is fixed or explicitly waived with a reason before the PR is opened.
- Completion criterion: both passes record "no unresolved P0".

### Phase 6 — Reproducibility audit, build, and PRs

#### T6.1 — Full rebuild and manifest
- Steps: run the §0.2 sequence end to end in a fresh shell; confirm `validate_artifacts.py` exits 0 (the two log-mtime checks now pass because open_research was rebuilt with `--keep-logs`); confirm `pdfinfo` page count and the page-9 check; commit `paper/iclr/build/main.pdf`, `paper/open_research/*.pdf`, `paper/results/publication_manifest.json`, `paper/results/artifact_manifest.json`, `paper/results/validation_summary.json`.
- Completion criterion: `validation_summary.json` shows `passed: true`.

#### T6.2 — Validator coverage for the ICLR sources (code)
- Add `validate_iclr_sources()` to `paper/scripts/validate_artifacts.py` and register it in `main()`:
  1. Every generated `paper/iclr/tables/*.tex` (from T2.x) is byte-identical to a fresh in-memory regeneration from its JSON.
  2. `sections/results.tex` contains the exact strings derived from `summary.json`: `90/90`, `89/90`, `66.6\%`, `63.1\%`, `64.2\%`, `58.7\%`, and each per-family reduction in `tables/primary_results.tex` equals `families[].reductions` rounded to 1 dp.
  3. `appendix.tex` contains `240/240` for baseline and learned gate and `239/240` for support-only, the string `160/240`, and does not contain `239/239`, `five-times`, `four times the prior`, `behaviour`, or `distribution-free`; `sections/*.tex` contain no bare decimal (`(=|<|>|\\le|\\ge)\s*\.\d`).
  4. The compiled PDF text contains no lowercase "proposition 1" or "corollary 2" and no `??`.
  5. `multiplicity_accounting.json` reports `U(92, m=2) = 0.056941…` and `n_min(m=2) = 106`; the abstract's certificate sentence matches the track recorded in `revision_log.md` (a literal string check on ABS-A vs ABS-B).
- Completion criterion: new check family present; CI green; deliberately breaking one number in a table makes the validator fail (documented in the PR).

#### T6.3 — Fix `ICLR_FIGURES` casing
- `paper/scripts/build_artifacts.py:33`: `PAPER / "ICLR" / "figures"` → `PAPER / "iclr" / "figures"`. Verify on a case-sensitive check: `git ls-files paper/ICLR` → empty; run `build_artifacts.py`; confirm the three PDFs in `paper/iclr/figures/` update.

#### T6.4 — Pull requests
- PR-1 (Phase 0–1 + T3.6 + T6.3, Track S, day 1): title "paper(iclr): repair Appendix H accounting, Algorithms 3–4, Appendix A multiplicity wording, and editorial ledger". PR-2 (Phase 2 + Phase 4 + Phase 5 + T6.1–T6.2, Track S, days 2–3): "paper(iclr): reanalysis tables, certificate hierarchy, dispatch coverage, reproducibility manifest, validator coverage". PR-3.. (Track R, one per experiment): "experiment: <protocol name>". Each PR follows `.github/pull_request_template.md` completely (Summary; Tests and validation with exact commands and outputs; Risks and follow-up; Copilot handoff boxes), documents goal, changes, approach, decisions, testing, risks, documentation/configuration implications, and links this plan. Request `rrahimi-uci` as reviewer; no Copilot review; leave open.
- Completion criterion: PRs open with green CI; `revision_log.md` lists every task id as done, waived, or deferred with the track.

---

## 4. Traceability matrix

| Reviewer concern | Evidence needed | Existing evidence | Gap | Required action | Output artifact | Paper location | Verification criterion |
|---|---|---|---|---|---|---|---|
| RC-1 multiplicity of candidate selection | Certificate level per artifact; m per family; corrected bound; repair cohort | Per-threshold tables for emitted artifacts (`compiler.artifact.gate.notes`); `compiler.report` candidate counts; issue-type `candidates[0].gate == null`; frozen flag in multirepo configs | Dominated candidates' tables not retained; no m parameter in `calibrate_gate`; no fresh cohort | T2.3 (recompile, accounting table), T4.1 (one certificate level), T3.1 (fresh 106-group cohorts, Track R) | `multiplicity_accounting.json/.tex`, recompiled candidate JSONs, `multiplicity_repair/*` | Abstract, §3, §5.1, §7, App A, App C | Table shows m=1 (issue-type, cross-repo) and m=2 with U=0.0569 (PR, backlog); one track used everywhere; T6.2 check passes |
| RC-2 239/240 vs 240/240 | Per-arm ITT and completed-case counts; failure identity | `aggregate.*.n`, `failures[0]` (#6708, support-only) | Text and Table 10 collapse arms | T1.1, T2.8 | regenerated `gate_frontier_disaggregated.tex`, `gate_frontier_reanalysis.json` | App H, Table 10, protocol .md, body.tex | Rows sum to 240/240, 240/240, 239/240; no `239/239` outside the completed-case sentence |
| RC-3 Algorithm 3 | Pseudocode matching Eq. (1) and the implementation | `provenance.py` marks; `windows.py` blocking; `i<j` edges | Pseudocode diverges | T1.4 | `alg-patg.tex`, `alg-compile.tex` edits | App B, Alg. 1 | Rendered Alg. 3 has `v_{j,s}`, `S[j,s]`, `s<t`, returns `(G,S)`; no "block region" |
| RC-4 Algorithm 4 | Bound inputs; explicit position check; defined capability; reachable branches | `dispatch.decide` signature; `EffectSpec.quota_attested`; staging semantics | No runtime position check beyond tool intersection; free variables in pseudocode | T1.5, T3.6 (code + tests) | `alg-dispatch.tex`, Block C-1, `dispatch.py` change, unit tests | App B, App C | Alg. 4 has `X.O ≠ ∅` line; tests pass; suite green |
| RC-5 Appendix A simulation wording | Correct scope of 1.8%–13.6% | Script unions m candidates at one threshold | Text reads grid-wide | T1.3 | Blocks A-1, A-2 | App A, `frozen_candidate_simulation.tex`, body.tex | "single-threshold" appears with both percentages; four-line γ/δ/m derivation present |
| RC-6 step gate over-interpreted | Statement of ⌈92/c⌉ constraint; q constancy; uncertifiable observed points | §3 already states the floor; `dispatch.q` constant; hf 10/92, balanced pandas 90/92 | App H and protocol read the null as evidence about q; protocol lacks the constraint | T1.2, T2.8, T4.3 | Block H-1, App C addition, F.4 rename | §3, §7, App C, App F.4, App H | No sentence attributes the step to q; protocol retrospective note added |
| RC-7 unguarded ablation | Guard-by-guard pricing; live recurrence-only arm | Nine retained hazards each caught by a named guard; natural-order 17/18 | No table; no live B1 | T2.10 (table), T3.2 (live arm, Track R), T3.3 (drift ablation, Track R) | `mechanism_removal.tex`, protocols, `recurrence_only_ablation/*`, `drift_ablation/*` | §6, §7, App G | Table has nine sourced rows; protocol committed before any spend; outcome sentence matches decision rule |
| RC-8 absolute metrics, CIs, discordance, medians | Per-record means/medians, cost CIs, issue-type rows, discordant cells, median latency | All per-record fields retained | Not computed/presented | T2.1, T2.2 | `absolute_resources.tex`, `paired_statistics.tex`, `discordance.tex` | §5.1 pointer, App D | Every CI cell filled; issue-type rows present; discordance table 1/0 |
| RC-9 reproducibility manifest | Immutable identifiers and policies | `run` blocks; code constants; price table | Scattered; "provider defaults" sentence | T2.5 | `live_provider_manifest.tex/.json`, Block REPRO-1 | App C, Reproducibility Statement | Manifest table present; "provider defaults" gone |
| RC-10 i.i.d. assumption | Qualifier placement; effective independent units | `min_days=min_principals=1`; calibration ids; parquet dates/authors | Counts never computed | T2.9, T4.4 | `effective_units.tex/.json` | §7, App C, App G | Qualifier in six places; counts stated |
| RC-11 90/90 vs 89/90 | Discordant cells; rerun of the same records | Record 5189; headroom rerun 30/30 | Text can be read as accuracy gain | T1.8 | Block R-1, Table 1 caption | Abstract, §5.1, App G | "baseline-only", "compiled-only", "McNemar", "5189", "30/30" present |
| RC-12 latency validity | Order design, concurrency, cache sharing, per-permutation sensitivity | `schedule[]`; `store=False`; cached tokens | Not described or analyzed | T2.4 | `order_sensitivity.json`, App D paragraph | App D, App C | Paragraph states design and min–max range |
| RC-13 live catalog trust root | Declarations, digests, audit | `make_catalog()`; manifest digests; 100% tool contracts | Not surfaced | T2.6 | `live_catalog.tex/.json`, barrier-band edit | App F/G, Fig. 2 | Table and paragraph present; "never a read" in Fig. 2 |
| RC-14 "independent five-times-larger" | Overlap counts | Selection ids | Wording false | T2.7, T1.1, T4.5 | `cohort_overlap_audit.json`, Blocks H-1, F1-1 | App H, App F.1 | "independent" only where overlap = 0 |
| RC-15 comparator positioning | Scoped Headroom, internal pricing | Headroom protocol; mechanism table | Over-reach | T4.2 | Block RW-1 | §6, §7, App G | "unguarded ablation unrun" removed; Headroom scoped |
| RC-16 read-only prologue | Protocol | App G conjecture | Unbuilt | T3.5 (Track F) | `read-only-prologue-protocol.md` | App G | File exists, cited |
| RC-17 larger frontier cohort | Protocol | ⌈92/c⌉ table | Unrun | T3.4 (Track F) | `graded-frontier-protocol.md` | App H | File exists, cited |
| RC-18 editorial ledger | — | — | Ten defects | T1.6, T1.10 | edits; regenerated Fig. 1 | throughout | Ten greps pass |
| RC-19 page budget | — | page-9 check | Insertions | T4.6 | cut ledger | §1–§8 | Conclusion ends on p. 9 |
| RC-20 OpenReview metadata | Final abstract, TL;DR | — | User action | T4.7 | `openreview_metadata.md` | OpenReview | Abstract equals compiled abstract |
| RC-21 psf/requests reuse | Timestamps, resolved_config | Retained | Undisclosed | T1.1 | Block H-1 | App H, protocol | Disclosure sentence present |
| RC-22 held-out dispatch coverage | `dispatch.outcome` counts | Retained | Unreported | T2.8, T4.5 | `multirepo_dispatch.tex` | §5.2, App F.1, App H | 80/120, 160/240, 180/180 stated |
| RC-23 support-only "identical" | Grid/U/threshold differences | `coverage_curves` | Caption wrong | T1.1 | new caption | Table 10 | Caption states δ/12, U=0.0507, threshold 1.0 |
| RC-24 4× scale, $0.41 | Ratios, cost sum | Retained | Wrong | T1.1 | Block H-1 | App H, protocol | "twice"; "$0.41 for successful attempts" |

---

## 5. Replacement text blocks (verbatim; adapt only bracketed track markers)

### Block ABS-B (abstract, Track B)
```latex
Admission certificates are exact finite-sample bounds under i.i.d.\ calibration
groups. They are compiler-wide wherever exactly one candidate reached
calibration (issue-type routing and every cross-repository family) and
per-candidate where two did (PR-outcome and backlog routing), whose
two-candidate correction lands at 0.057, just above the 0.05 budget.
```
### Block ABS-A (abstract, Track A — only after T3.1 admits both families)
```latex
Admission certificates are exact finite-sample bounds under i.i.d.\ calibration
groups, corrected over every candidate that reached calibration; on fresh
106-group cohorts every reported primary artifact satisfies the compiler-wide
0.05 bound.
```

### Block M-1 (§3, paragraph after Proposition 1)
```latex
\Cref{app:proof} gives the proof. The guarantee is per fixed candidate and
conditional on i.i.d.\ groups, which the cohorts assume rather than establish
(\cref{sec:discussion}). Conditional on train and development data the number
$m$ of candidates reaching calibration is fixed, so a run is compiler-wide when
$m=1$ (\cref{cor:frozen}) or when $\gamma$ is divided by $m$; \cref{app:config}
reports $m$ and both bounds for every artifact. Guarded deployment requires an
explicit evidence boundary, not just a recurrent trace and a successful replay.
```

### Block R-1 (§5.1, first two paragraphs)
```latex
Across the 90 held-out records, \method satisfies 90/90 exact contracts and the
unchanged agent 89/90 (\cref{tab:primary}), reducing provider requests by
66.6\%, tokens by 63.1\%, latency by 64.2\%, and cost by 58.7\% (absolute
per-record values in \cref{tab:absolute-resources}). Issue-type routing admits
only a two-read prefix; the two deeper workflows approach 75\% fewer requests.

The discordant pair is one baseline-only miss --- backlog record 5189, whose
unchanged agent skipped the discussion read and returned an ungrounded excerpt
--- against zero compiled-only misses (exact McNemar $p=1$; one-sided 95\% upper
bound on compiled-only discordance 3.3\%, \cref{tab:discordance}). The same 30
records rerun on 2026-08-18 scored 30/30 in every arm (\cref{app:limits}), so
the miss is a stochastic baseline event; the result supports preservation on
this cohort, not superiority or equivalence in general. Each artifact was
admitted with 92 zero-violation calibration groups
($U_{\hat\eta}=0.0498\le0.05$), a bound conditional on i.i.d.\ groups.
% TRACK B:
The certificate is compiler-wide for issue-type routing, where one candidate
reached calibration, and per-candidate for PR-outcome and backlog routing,
where two did; correcting for both gives $U=0.0569$, above $\alpha$, so those
two are certified compiler-wide only at $\alpha\ge0.057$ (\cref{cor:frozen},
\cref{tab:multiplicity-accounting}).
% TRACK A (replace the TRACK B sentence):
% The certificate is compiler-wide for all three: issue-type routing calibrated
% one candidate, and PR-outcome and backlog routing satisfy the two-candidate
% correction on fresh 106-group cohorts (\cref{tab:multiplicity-repair}).
```

### Block D-1 (§7, opening paragraph)
```latex
\paragraph{Three constraints bound the claim.} First, admission control is
compiler-wide only when one candidate reaches calibration or the confidence
budget is divided across all that do. That holds for issue-type routing and for
every cross-repository family; PR-outcome and backlog routing calibrated two
candidates on 92 groups, where the corrected bound is 0.057 (\cref{app:config})
% TRACK A: and hold at $\alpha=0.05$ after the fresh 106-group calibration of \cref{app:config}
. Second, with $|\mathcal{K}|=92$ the registered exact bound certifies only
full coverage, so no experiment here demonstrates graded selectivity of $q$: the
prospective protocol (\cref{app:prospective}) found the learned gate and a
support-only comparator indistinguishable across five repositories and 240
held-out triples, as they must be at this cohort size, and $q$ is constant
within a repository there because the entry state is one integer. Third, the
binomial result is conditional on i.i.d.\ groups: the 92-record live calibration
cohorts span [N_DAY_MIN–N_DAY_MAX] distinct creation days and
[N_AUTHOR_MIN–N_AUTHOR_MAX] distinct authors per family
(\cref{tab:effective-units}), and neither corpus establishes exchangeability
under temporal or principal clustering; a seeded simulation (\cref{app:proof})
prices the exposure. The follow-up is to buy calibration groups first: below
$\lceil 92/c\rceil$ of them no refit of $q$ can be read.
```
Replace `[N_DAY_MIN–N_DAY_MAX]` and `[N_AUTHOR_MIN–N_AUTHOR_MAX]` with the ranges from `effective_units.json` (T2.9).

### Block RW-1 (§7 sentence and Appendix G sentences)
§7, replacing "…and run no head-to-head … leaving the \emph{unguarded} ablation unrun.":
```latex
and run no head-to-head against the workflow-reuse line, whose prompt-level
induction, parallel scheduling, and plan caching make no admissibility decision
to compare against. We price the guards internally instead:
\cref{tab:mechanism-removal} lists nine retained hazards and the guard that
caught each, and a recurrence-only live arm is pre-registered on the one family
where it differs from the compiled program (\cref{app:limits}).
```
Appendix G, replacing the last two sentences of "Why the workflow-reuse line is not a drop-in comparator":
```latex
The comparison we owe is internal. \Cref{tab:mechanism-removal} prices the
barriers: for each retained hazard it names the guard that caught it and what
deterministic replay of the mined region would have done with provenance,
effect, position, contract, and risk barriers disabled. On PR-outcome and
backlog routing the mined three-read sequence \emph{is} the admitted program, so
an unguarded replay executes the same calls and differs only in guards that
never fired on in-distribution held-out records; on issue-type routing the
compiler refused the three-read candidate, and a pre-registered live
recurrence-only arm
(\code{paper/supplementary/recurrence-only-ablation-protocol.md}) measures that
refusal's price directly.
% TRACK R, after T3.2: replace the last clause with the observed outcome in the
% words of the T3.2 decision rule.
```

### Block F1-1 (§5.2 body and Appendix F.1 paragraph)
§5.2:
```latex
On a narrower two-read pull-request outcome task across frozen public
repositories, with held-out records strictly newer than discovery ones and
selection sealed before any provider call, \method completes 120/120 exact
pairs on four of five repositories and retires \code{pytorch/pytorch} rather
than loosening its gate (\cref{app:breadth}). The compiled program ran on 80/120
held-out records: every open-class record abstained on the induced verifier's
\code{pr.state} hull, learned from discovery pools holding 0--3 open records,
and ran the unchanged agent, so requests fall 44.4\% overall (66.7\% on
dispatched records) and tokens 52.4\%. A balanced three-repository rerun, whose
discovery pools contain all three classes, dispatches on 180/180 and reaches
180/180 exact pairs at 66.7\% fewer requests and 78.4\% fewer tokens
(\cref{tab:multirepo-disaggregated}). A fixed two-read template is more
efficient in the core protocol and essentially tied in the rerun: evidence of
discovery and principled retirement, not of runtime dominance.
```
Appendix F.1, new paragraph before `tab:multirepo-disaggregated`:
```latex
\paragraph{Held-out dispatch and cohort provenance.}
\Cref{tab:multirepo-dispatch} separates calibration coverage from held-out
dispatch. In the core protocol the artifact dispatched on 20 of 30 records in
every repository; the ten open-class records abstained with reason
\code{range:pr.state}, because the verifier's categorical hull, induced on
discovery pools with 2, 0, 3, and 0 open records, contained only
\code{closed}, and the abstention is a clean fallback that runs the unchanged
three-request agent. The balanced rerun's 40/40/40 discovery pools induce the
hull $\{\code{closed},\code{open}\}$ and dispatch 60/60 per repository, which is
the whole difference between 44.4\% and 66.7\%. The core summary was produced
in two invocations: the first, with a five-repository completeness gate,
stopped when \code{pytorch/pytorch} retired; the second accepted four
repositories and 120 pairs and reused the four completed per-repository
results unchanged. The three cross-repository cohorts are drawn from the same
frozen snapshots without mutual exclusion: the balanced rerun shares 13, 25,
and 8 of the 30-record core held-out sets, and the gate-frontier cohort of
\cref{app:prospective} shares 130/150 held-out and 442/460 calibration records
with the core cohort (\code{paper/results/iclr\_revision/cohort\_overlap\_audit.json}).
```

### Block H-1 (Appendix H, `\subsection{Observed results}` body)
```latex
The sealed cohort targets 300 pooled held-out pairs across the five repositories
\cref{sec:res-multirepo} already uses, 116 discovery and 60 held-out cases each
under frozen-candidate selection. Discovery reaches 580/580 exact traces. Four
repositories admit an artifact; \code{pytorch/pytorch} again retires at compile
time. This cohort is drawn from the same frozen snapshots with the same
selection seed as the core study and excludes none of its records, so it shares
561/580 discovery, 442/460 calibration, and 130/150 held-out records with the
core cohort; at twice the core scale it is a larger sealed re-execution, not an
independent replication, and the \code{pytorch/pytorch} retirement is a
near-replay of the core one on a 93--95\% identical split. The pooled result is
therefore 240 of the pre-registered 300 pairs, short of target for the stated
reason.

\textbf{Accounting.} 240 records $\times$ 3 arms = 720 attempted episodes, 719
completed. Exact task contracts under intention-to-treat: unchanged agent
240/240, learned gate 240/240, support-only 239/240. The incomplete episode is
\code{psf/requests} record 6708 under the support-only arm, which raised the
120\,s provider timeout; the same record completed and passed in the other two
arms. The protocol fixed no retry rule, so the episode was not re-attempted and
counts as a failure; among the 239 records with three completed arms every arm
passes 239/239. Two provenance facts belong here. The \code{psf/requests} block
was executed once, on 2026-08-22T18:59Z, as the single-repository pipeline
check under the sealed selection; the five-repository run reused it through the
driver's resume path rather than re-executing it. Retained spend for successful
attempts sums to \$0.41; the two failed \code{streamlit/streamlit} discovery
attempts are not retained.

\textbf{Dispatch.} Calibration coverage is 1.0 in every admitting repository,
but held-out dispatch is not: the compiled program ran on 160/240 records
(learned gate) and 159/239 (support-only). The other 80 --- every open-class
pull request, 20 per repository --- abstained on the induced verifier's
\code{pr.state} hull, which the discovery pools (0--3 open records each) had
fixed at $\{\code{closed}\}$, and ran the unchanged agent. Relative to baseline
the learned gate reduces provider requests 44.4\%, total tokens 52.3\%,
observed wall latency 51.0\%, and estimated cost 48.3\%; the support-only gate
--- $\alpha=1$ in an otherwise identical second \code{compile\_grc} pass ---
44.4\%, 52.4\%, 47.9\%, and 48.8\%. Both arms admit the same artifact; they
differ in $\alpha$, in the grid ($\alpha=1$ appends $\eta=1.0$, so the split is
$\delta/12$ and $U=0.0507$ at $n=92$), and in the deployed threshold (1.0
versus 0.5).

\textbf{Coverage levels.} \Cref{tab:gate-frontier-disaggregated} lists every
coverage the frozen grid produces. Three admitting repositories accept no
calibration group below $\eta=0.11$ and all 92 at $\eta\ge 0.11$;
\code{huggingface/datasets} additionally accepts 10/92 at $\eta\le 0.08$, whose
exact upper bound 0.375 exceeds $\alpha$. No repository deploys at an
intermediate coverage --- and none could: with $|\mathcal{K}|=92$ the
registered bound certifies only $n_\eta=92$, so the pre-registered criterion of
three admissible nonzero coverage levels was unattainable at this cohort size
whatever $q$ ranks, and the protocol should have said so. Held-out $q$ is
moreover constant within each repository (0.0169; 0.0815), because the entry
state is a single record number and every entry-observable feature is a
function of it. Per the decision rule fixed above in this appendix, this is the
fourth listed outcome: neither gate produces graded coverage. We report it as a
null about the design, not as evidence about $q$: the study audits
support-threshold behavior at full coverage under two risk budgets and does not
test the ranking quality of $q$. The protocol that could
(\code{paper/supplementary/graded-frontier-protocol.md}) needs
$|\mathcal{K}|\ge\lceil 92/c\rceil$ groups per repository and an entry state
with informative features; it is written and not run.
```

### Block A-1 (Appendix A, replaces the simulation paragraph)
```latex
\paragraph{Multiplicity accounting.} For one fixed candidate and one fixed
threshold $\eta$, $\Pr(r_\eta>U_\eta)\le\gamma=\delta/|\Lambda|$. For one fixed
candidate over the whole grid, the union bound gives
$\Pr(\exists\eta\in\Lambda:r_\eta>U_\eta)\le|\Lambda|\gamma=\delta$. For a set
of $m$ candidates fixed before calibration and each evaluated over $\Lambda$,
choosing $\gamma=\delta/(m|\Lambda|)$ gives
$\Pr(\exists\,a,\eta: r_{a,\eta}>U_{a,\eta})\le\delta$; at $\alpha=0.05$,
$\delta=0.1$, $|\Lambda|=11$ the zero-violation requirement is 92 groups for
$m=1$ and 106 for $m=2$ ($U=0.0498$ versus $0.0569$ at $n=92$).
\code{paper/scripts/frozen\_candidate\_coverage\_simulation.py} checks the
\emph{single-threshold} quantity exactly, without Monte Carlo: for one candidate
at one threshold and $n=92$, the population rate that maximizes miscoverage is
$r^{*}\approx0.637$, where miscoverage is $\approx0.00909\approx\gamma$. Pooling
$m$ such candidates at the uncorrected per-candidate budget raises that
single-threshold union to 1.8\% at $m=2$ and 13.6\% at $m=16$, while the
corrected budget keeps it below 0.81\% for every $m$ tested. These are
illustrative single-threshold figures; the grid-wide and candidate-wide
certificates are the $\delta$ statements above, not these numbers. A companion
seeded Monte Carlo (seed \code{20260822}, $200{,}000$ replicates per cell) draws
the 92 groups in correlated blocks instead of i.i.d.: realized miscoverage
matches the exact value at zero correlation and reaches 56\% against the 0.9\%
nominal single-threshold budget at the most clustered configuration tested
(block size 46, one quarter of blocks shifted by 0.3), which is why the live
studies' \code{min\_days}=\code{min\_principals}=1 configuration is this
guarantee's weakest link.
```

### Block A-2 (Appendix A, replaces "It is not hypothetical: … not retroactively strengthened.")
```latex
Conditional on train and development data, the number $m$ of candidates that
reach calibration is fixed before any calibration label is read. The
cross-repository extension runs with freezing enabled and every sealed
repository report shows one candidate reaching calibration, so \cref{cor:frozen}
applies to those admissions directly. Issue-type routing did not freeze, but its
second candidate retired at synthesis (an ungroundable slot) and never saw
calibration, so $m=1$ and the same argument applies. PR-outcome and
backlog-attention routing each calibrated $m=2$ candidates on the same 92
groups and kept the higher-removal survivor: their certificates are
per-candidate at $\alpha=0.05$, and under $\gamma=\delta/(m|\Lambda|)$ the
retained zero-violation tables give $U=0.0569>\alpha$, a compiler-wide
certificate only at $\alpha\ge0.057$ or after 106 groups
(\cref{tab:multiplicity-accounting}).
% TRACK B:
A fresh 106-group calibration under the pre-registered protocol
\code{paper/supplementary/multiplicity-repair-protocol.md} is the repair; it is
pre-registered and not yet run.
% TRACK A: The fresh 106-group calibration of \cref{tab:multiplicity-repair}
% supplies that certificate at $\alpha=0.05$ for both.
```

### Block C-1 (Appendix C, new paragraph after "Admission budgets")
```latex
\paragraph{Runtime capability vocabulary.} \code{quota\_attested} is a boolean
field of a tool's signed effect declaration, not a capability flag: it marks a
read whose invocation advances a counter (a rate quota or an audit log) that
the staging snapshot must attest over before an attempt can be discarded as
clean, so in live mode such a tool is not dispatched without a snapshot
provider (\cref{alg:dispatch}). Position~0 of \cref{eq:prefix} is identified at
runtime with ``no tool observation has been committed in this episode'': any
earlier tool call, whether or not the compiled program uses that tool, returns
the unchanged agent, the runtime form of the compile-time \code{prefix\_only}
constraint. In the shipped facade every effect outside
$\{\textsc{pure},\textsc{read-local},\textsc{read-external}\}$ is refused at
commit and raises an incident; no post-commit path is reachable in the
reported runs, and \cref{alg:dispatch}'s commit line is that contract.
```

### Block REPRO-1 (Reproducibility Statement, append)
```latex
The artifact pins the provider model string, the \code{openai-agents} and
\code{openai} SDK versions, the effective generation settings, cache policy,
the 120\,s timeout and no-retry policy, the per-record condition order,
concurrency, the token fields used for cost, the rate-card date, the tool and
effect-catalog digests, and every split and candidate manifest
(\cref{tab:live-manifest}).
```

### Block TLDR (OpenReview)
```
Compile recurrent read-only agent prefixes into guarded programs only when provenance, declared effects, position, induced contracts, and an exact finite-sample bound admit them; otherwise retire or fall back to the unchanged agent.
```

### Block ALG-3 (`paper/iclr/figures/alg-patg.tex`, full replacement)
```latex
\begin{algorithm}[t]
\caption{\textsc{BuildPatg} --- typed argument provenance. \patg annotates every
argument slot and blocks nothing itself; \textsc{MineRegions} (\cref{alg:compile})
rejects any window containing an \textsc{ungrounded} or \textsc{ambiguous} slot,
so a slot with no witness (a genuine model decision) and a slot with too many
witnesses both retire the region rather than defaulting to the cheapest
explanation.}
\label{alg:patg}
\footnotesize
\begin{algorithmic}[1]
\Require episodes $\mathcal{T}$; entry schema $\Theta$; effect catalog $\mathcal{C}$;
         transform library $\mathcal{L}$; ambiguity cap $\kappa$
\Ensure provenance graph $G$ (witness and order edges) and slot-status map $S$
\State $G\gets\emptyset$;\quad $S\gets\emptyset$
\ForAll{episodes $E=(z,M,e_{1:T},y)\in\mathcal{T}$}
  \State $\Sigma\gets\{(\text{``}z\text{''},\pi,v):(\pi,v)\in\textsc{Flatten}(z),\ \pi\in\Theta\}$
         \Comment{only allowlisted entry paths are eligible}
  \ForAll{call--result pairs $(c_j,o_j)$ in $e_{1:T}$, in temporal order}
    \ForAll{slot names $s\in\operatorname{keys}(\operatorname{args}(c_j))$}
      \State $v_{j,s}\gets\operatorname{args}(c_j)[s]$ \Comment{the slot's value, distinct from its name}
      \If{$(c_j,s)$ is declared literal-only in $\Theta$}
        \State $S[j,s]\gets\textsc{literal}$; \textbf{continue}
               \Comment{preserve the declared constant; infer no provenance}
      \EndIf
      \State $\mathcal{H}\gets\textsc{BestPerSource}\{(\sigma,g)\in\Sigma\times\mathcal{L}:
             g(\operatorname{value}(\sigma))=v_{j,s},\ \operatorname{depth}(g)\le 2\}$
      \If{$\mathcal{H}=\emptyset$}
        \State $S[j,s]\gets\textsc{ungrounded}$ \Comment{no witness: a model decision}
      \ElsIf{$|\mathcal{H}|>\kappa$}
        \State $S[j,s]\gets\textsc{ambiguous}$
      \Else
        \State $S[j,s]\gets\textsc{grounded}$;\quad
               $G\gets G\cup\{\sigma\xrightarrow{\,g\,}(j,s):(\sigma,g)\in\mathcal{H}\}$
               \Comment{$1..\kappa$ single-source witnesses; synthesis commits to one}
      \EndIf
    \EndFor
    \State $\Sigma\gets\Sigma\cup\{(\operatorname{var}(c_j),\pi,v):
           (\pi,v)\in\textsc{Flatten}(o_j),\ \textsc{Eligible}(\pi,v)\}$
           \Comment{$o_j$ witnesses only \emph{later} slots}
  \EndFor
  \ForAll{event pairs $(e_s,e_t)$ with $s<t$ sharing a resource where at least one writes it}
    \State $G\gets G\cup\{e_s\prec e_t\}$ \Comment{order edge in observed temporal order}
  \EndFor
\EndFor
\State \Return $(G,S)$
\end{algorithmic}
\end{algorithm}
```

### Block ALG-4 (`paper/iclr/figures/alg-dispatch.tex`, full replacement)
```latex
\begin{algorithm}[t]
\caption{\textsc{Dispatch} --- boundary-time admission. Cheap deterministic checks
reject first and the calibrated gate runs only on what survives them. Exactly one
exit path compacts; clean rejections return the unmodified agent, while an
unattested failure or a refused commit raises an incident.}
\label{alg:dispatch}
\footnotesize
\begin{algorithmic}[1]
\Require entry state $z$; runtime context $X=(M',\mathcal{O},\textsc{snap},\textsc{stage})$
         --- runtime manifest, tools already observed in this episode, optional
         snapshot provider, staging owner; registry $\mathcal{R}$; effect catalog
         $\mathcal{C}$; mode $m\in\{\textsf{off},\textsf{shadow},\textsf{live}\}$
\Ensure observations for the region, \fallback{} to baseline agent $B$, or \textsc{Incident}
\If{$m=\textsf{off}$} \State \Return \fallback
    \Comment{conformance: model-visible input must be byte-identical}
\EndIf
\State $A\gets\mathcal{R}.\textsc{Resolve}(X.M'.\text{compatibility},X.M'.\text{partition})$
\If{$A=\bot \ \vee\ A.\text{lifecycle}\neq\textsc{Active}
     \ \vee\ \neg\textsc{VerifySignature}(A)$}
  \State \Return \fallback
\EndIf
\If{$X.\mathcal{O}\neq\emptyset$} \State \Return \fallback
    \Comment{\cref{eq:prefix}: any earlier tool call, by any tool, leaves position~0}
\EndIf
\If{$H_A(z,X.M')\neq 1$} \State \Return \fallback
    \Comment{manifest pins, isolation keys, typed hulls, effect set}
\EndIf
\If{$m=\textsf{live} \ \wedge\ \exists f\in\operatorname{tools}(A.P):
     \mathcal{C}(f).\textit{quota\_attested} \wedge X.\textsc{snap}=\bot$}
  \State \Return \fallback
    \Comment{a quota-bearing read cannot be discarded cleanly without a snapshot}
\EndIf
\If{$q_A(z)>\eta_A$} \State \Return \fallback
    \Comment{calibrated abstention on an uncertain input}
\EndIf
\If{$m=\textsf{shadow}$}
  \State \textbf{log} would-dispatch; \Return \fallback
    \Comment{observe coverage without changing behavior}
\EndIf
\State $S\gets X.\textsc{stage}.\textsc{Begin}(X.\textsc{snap})$;\quad
       $r\gets\textsc{Interpret}(A.P,z,\textsc{Facade}(\mathcal{C}))$
       \Comment{the facade re-checks effects at execution time}
\If{$r$ raised a pre-commit error}
  \State \Return \fallback{} if $S.\textsc{AbortAndAttest}()$, else \textsc{Incident}
\EndIf
\If{$V_A(r)\neq 1$}
  \State \Return \fallback{} if $S.\textsc{AbortAndAttest}()$, else \textsc{Incident}
    \Comment{live-outs, effect multiset, and call count are checked before use}
\EndIf
\State \Return $r.\text{observations}$ if $S.\textsc{Commit}(r.\text{effects})$
       succeeds, else \textsc{Incident}
       \Comment{commit refuses any effect outside the read classes; a refused commit is never a fallback}
\end{algorithmic}
\end{algorithm}
```

---

## 6. Vague-instruction audit (second pass)

Every instruction in the feedback blueprint that read as a direction rather than an action was replaced as follows. Executors must not reintroduce the left-hand phrasing into task notes or PR text.

| Blueprint phrasing | Concrete replacement in this plan |
|---|---|
| "Repair or sharply delimit candidate-selection multiplicity" | T2.3 computes U(92, m=2)=0.0569 from retained grid rows and recompiles the dominated candidates; T4.1 fixes one certificate sentence per surface (Blocks ABS-B/A, M-1, R-1, D-1, A-2); T3.1 specifies the fresh 106-group cohort with candidate ids, seed, class draw, spend, decision rule |
| "Upgrade metric reporting" | T2.1 (absolute means/medians, exact expected values), T2.2 (cost CIs, issue-type rows, discordance table, median latency with CI), T2.4 (order sensitivity), T2.5 (manifest table with named keys) |
| "Reframe the gate evidence" | T1.2, T4.3: exact sentences (Block H-1 "Coverage levels", F.4 rename, App C two uncertifiable points, §7 sentence with the two named reasons) |
| "Add an unguarded recurrence-only ablation" | T2.10 (nine-row table with file sources), T3.2 (issue-type-only live arm: plan construction, 30 records, spend $0.25 ceiling, three hypotheses, decision rule), T3.3 (existing drift protocol, driver name, outputs) |
| "Make the i.i.d. group assumption operational" | T2.9 (join calibration ids to parquet; n_eff by day/author/month; U at n_eff; table), T4.4 (six qualifier locations) |
| "Strengthen the effect-catalog trust story" | T2.6 (serialize `make_catalog()`, digest match, call counts, 100% contract audit, "never a read" in Fig. 2) |
| "Upgrade live-provider reproducibility" | T2.5 twelve-row manifest with sources; sentence replacement at `appendix.tex:112-113`; Block REPRO-1 |
| "Update claim hierarchy" | T4.1 step list and T5.1 grid with grep commands |
| "Comparator positioning" | T4.2 four edits with exact locations; Block RW-1 |
| "Final hostile review" | T5.4: two single-question passes, findings logged, fixed or waived |
| "Recompute metrics … all tables regenerate from retained raw files" | one script `iclr_revision_statistics.py` with sub-commands, one test file, T6.2 validator regeneration check |
| "Fix Algorithms 3 and 4" | Blocks ALG-3, ALG-4 verbatim; T3.6 code change with three named tests |
| "Correct the 239/240 vs 240/240 reporting" | Block H-1 "Accounting" paragraph and the new Table 10 layout with per-arm columns |
| "Implement a recognized read-only prologue (only if time permits)" | T3.5: protocol file only, five named acceptance tests, not implemented |
| "Keep new tables and configuration detail in appendices" | T4.6 line ledger with five pre-approved cuts and the page-9 verification command |

---

## 7. Execution order and effort

| Day | Tasks | Gate to proceed |
|---|---|---|
| 0 | T0.1, T0.2 | baseline green; log created |
| 1 | T1.1–T1.6, T1.8, T1.10, T3.6, T6.3 → PR-1 | ICLR PDF builds; page 9 holds; `validate_artifacts.py` unchanged failures only |
| 2 | T2.1–T2.10 (script + tests), T2.11 if time | all pins pass; JSON + tables generated |
| 3 | T4.1–T4.7, T5.1–T5.4, T6.1, T6.2 → PR-2 | validator exits 0; conclusion on page 9; anonymity checks pass; number registry complete |
| before deadline | user pastes OpenReview metadata from T4.7; submission PDF = `paper/iclr/build/main.pdf` at PR-2 head | — |
| Track R, days 4–10 | T3.1 (protocol → preflight → run → recalibrate → Track A switch if admitted), T3.2, T3.3 → PR-3.. | each protocol committed before its first provider call; results directories new |
| Track F | T3.4, T3.5 protocol files (can be written on day 2 and cited in PR-2) | — |

Total expected provider spend for Track R: ≤ $1.50 (T3.1 ≤ $1.00 across two families, T3.2 ≤ $0.25, T3.3 $0). Every other task in this plan is provider-free.

Definition of done for the whole plan: every RC row in §4 has its verification criterion satisfied or an explicit `revision_log.md` entry stating "deferred to Track R/F" with the protocol file path; `validate_artifacts.py` exits 0 with the new `validate_iclr_sources` family; the compiled ICLR PDF states one certificate level everywhere; PR-1 and PR-2 are open for review and unmerged.
