\section{Results}

\subsection{Primary workflows: preserved contracts, less work}
\label{sec:res-primary}

\begin{table}[t]
  \centering
  \caption{Primary live-provider results ($n=30$ held-out records per family).
  ``Interfaces'' counts provider-visible tool interfaces. All conditions see
  identical records, model, and grader; \emph{Manual} is hand-written with full
  knowledge of the workflow, and \cref{tab:manual-resources} in \cref{app:stats} prices it on these
  same resource axes.}
  \label{tab:primary}
  \footnotesize
  \setlength{\tabcolsep}{3.5pt}
  \input{tables/primary_results.tex}
\end{table}

Across the 90 held-out records, \method satisfies 90/90 exact contracts and the
unchanged agent 89/90 (\cref{tab:primary}), reducing provider requests by
66.6\%, tokens by 63.1\%, latency by 64.2\%, and cost by 58.7\% (absolute
per-record values in \cref{tab:absolute-resources} in \cref{app:stats}). Issue-type routing admits
only a two-read prefix; the two deeper workflows approach 75\% fewer requests.

The discordant pair is one baseline-only miss (backlog record 5189, an
ungrounded excerpt) against zero compiled-only misses (McNemar $p=1$; one-sided
95\% upper bound on compiled-only discordance 3.3\%, \cref{tab:discordance} in \cref{app:stats});
the same 30 records rerun on 2026-08-18 scored 30/30 in every arm
(\cref{app:limits}), so the miss is a stochastic baseline event and the result
supports preservation on this cohort, not superiority or equivalence. Each artifact was
admitted with 92 zero-violation calibration groups
($U_{\hat\eta}=0.0498\le0.05$), a bound conditional on i.i.d.\ groups. The
certificate is compiler-wide for issue-type routing, where one candidate
reached calibration, and per-candidate for PR-outcome and backlog routing,
where two did; correcting for both gives $U=0.0569$, above $\alpha$, so those
two are certified compiler-wide only at $\alpha\ge0.057$ (\cref{cor:frozen},
\cref{tab:multiplicity-accounting} in \cref{app:config}).

Hand-written programs also reach 90/90. \Cref{tab:manual-resources} prices them
on the same axes: they tie on requests, issue fewer interfaces, are marginally
cheaper in tokens and dollars, and lose only on latency.
We claim discovery and admission, not runtime dominance over hand-written
code.

\subsection{Time-forward transfer: compile or retire}
\label{sec:res-multirepo}

On a narrower two-read pull-request outcome task across frozen public
repositories, with held-out records strictly newer than discovery ones and
selection sealed before any provider call, \method completes 120/120 exact
pairs on four of five repositories and retires \code{pytorch/pytorch} at
admission, where no calibration group is accepted at any threshold, rather
than loosening its gate (\cref{app:breadth}). The compiled program ran on 80/120
held-out records: every open-class record abstained on the induced verifier's
\code{pr.state} hull, learned from discovery pools holding 0--3 open records,
and ran the unchanged agent, so requests fall 44.4\% overall (66.7\% on
dispatched records) and tokens 52.4\%. A balanced three-repository rerun, with all three
classes in discovery, dispatches on 180/180 and reaches 180/180 exact pairs at 66.7\% fewer requests and 78.4\% fewer tokens
(\cref{tab:multirepo-disaggregated} in \cref{app:breadth}). A fixed two-read template is more
efficient in the core protocol and essentially tied in the rerun: evidence of
discovery and principled retirement, not of runtime dominance.

\subsection{What binds: sample size, then deployability}
\label{sec:res-refusal}

\begin{table}[t]
  \centering
  \caption{Where refusal happens, and what happens where it does not. Stage
  numbers are \cref{alg:compile}'s; ``slots'' is the share of dependency slots with
  a recovered producer, and $n_{\max}$ is the largest family support against the 92
  \cref{eq:cp} requires. ``p/a/w'' is pass/abstain/wrong on each substrate's
  \emph{family-level} replay split, which is held out from a different fit than
  the admission split quoted in the text (\cref{app:external}).}
  \label{tab:funnel}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \input{tables/refusal_funnel.tex}
\end{table}

The external substrates isolate the binding constraint (\cref{tab:funnel}). All
four clear provenance, synthesis, and replay with \emph{no} wrong execution,
then NESTFUL, API-Bank, and executed BFCL gold plans retire --- each settled
arithmetically before the compiler ran, which is why a fourth was needed. AppWorld's gold solutions reach $n_{\max}=136$, making it the first
external corpus where \cref{eq:cp} is satisfiable, and it admits one artifact at
92/92 zero-violation groups ($U_{\hat\eta}=0.0498$) with 11/11 exact replay on
its sealed admission split, under both pre-registered effect arms. Two features
bound it: the gate is step-like, and the admitted program is argument-free, so
provenance fixed its \emph{length} rather than its content
(\cref{app:external}).

Admissibility is not dispatchability. Gold solutions are not agent trajectories,
so we also test the admitted region against AppWorld's 28 released baseline
runs. Over 8{,}190 trajectories it is structurally eligible at the entry boundary in
$37.9\%$, and the architecture explains the spread (\cref{tab:appworld-dispatch} in \cref{app:external}):
full-code agents are eligible almost always, ReAct and plan-and-execute almost
never, because they open by reading API documentation and the prefix leaves
position~0. Trajectories repeat tasks across the 28 runs, so they are descriptive units,
not independent samples, and the measurement bounds $\varphi$ from above.
