\begin{algorithm}[t]
\caption{\textsc{Calibrate} --- fixed-grid exact selective admission. The score $q$ and
the grid $\Lambda$ are frozen \emph{before} this procedure sees a calibration group,
which is what makes the union bound legitimate.}
\label{alg:calibrate}
\scriptsize
\begin{algorithmic}[1]
\Require frozen candidate $A_0=(P,H,V,q,M)$ without threshold; calibration
         groups $\mathcal{K}$ with fixed per-instance violations $L(A_0,x)$;
         grid $\Lambda$; budgets $\alpha,\delta$
\Ensure threshold $\eta$ with a simultaneous guarantee, or $\bot$ (\retire)
\State $\Pi\gets\emptyset$ \Comment{admissible (coverage, threshold) pairs, distinct from \cref{alg:compile}'s $\mathcal{A}$}
\ForAll{$\eta\in\Lambda$}
  \State define $d^\eta(x)$ by Eq.~\eqref{eq:dispatch} with threshold $\eta$;
         $D^\eta(g)\gets\max_{x\in g}d^\eta(x)$;
         $W^\eta(g)\gets\max_{x\in g}d^\eta(x)L(A_0,x)$
  \State $K_\eta\gets\{\,g\in\mathcal{K}:D^\eta(g)=1\,\}$;\quad
         $n_\eta\gets|K_\eta|$;\quad
         \textbf{if} $n_\eta=0$ \textbf{then continue}
         \Comment{vacuous bound $U_\eta=1$}
  \State $k_\eta\gets|\{\,g\in K_\eta:W^\eta(g)=1\,\}|$
         \Comment{\emph{wrong after dispatch} only, never abstention}
  \State $U_\eta\gets 1$ if $k_\eta=n_\eta$, else
         $\operatorname{Beta}^{-1}\!\big(1-\tfrac{\delta}{|\Lambda|};\,
         k_\eta+1,\ n_\eta-k_\eta\big)$;\quad
         \textbf{if} $U_\eta\le\alpha$ \textbf{then}
         $\Pi\gets\Pi\cup\{(n_\eta/|\mathcal{K}|,\eta)\}$
         \Comment{Eq.~\eqref{eq:cp}}
\EndFor
\State \Return $\bot$ if $\Pi=\emptyset$, else the $\eta$ component of
       $\operatorname*{lexmax}_{(\mathrm{cov},\eta)\in\Pi}
       (\mathrm{cov},\eta)$
       \Comment{largest empirical group coverage; larger threshold breaks ties}
\end{algorithmic}
\end{algorithm}
