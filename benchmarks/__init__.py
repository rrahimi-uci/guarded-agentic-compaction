"""Provider-free benchmark protocol and domain implementations.

This package is intentionally outside the installable library surface.  Generic
contracts belong in :mod:`agent_compaction`; public-data acquisition, adapters,
and study manifests remain benchmark artifacts.
"""
